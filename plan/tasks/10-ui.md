# Task 10 — UI
Goal: static SVG/JS page — topology editor (runs, gates, base intervals, elevation), generate button, strategy overlay, inspector w/ explanations, knowledge/annotations/review/BOM tabs.
Dependencies: 09. Acceptance: manual runbook walk of S01/S05/S10/S11/S14 via UI; no domain logic in JS.
Validation: uv run pytest tests/api -q && manual runbook (docs/v1-runbook.md)
