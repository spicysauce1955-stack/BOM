"""What may be DONE to a job — the closed, typed, permission-checked table.

A pure leaf: stdlib and pydantic, nothing else. It has to be, because both ends
of the system reach it. A route performs a row; an agent proposes one. If this
package could import either, one of them would end up owning it again.
"""
