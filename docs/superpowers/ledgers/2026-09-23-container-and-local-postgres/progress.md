# SDD ledger — plan: docs/superpowers/plans/2026-09-23-container-and-local-postgres.md

Worktree: `.claude/worktrees/slice3-container-postgres`, branch
`worktree-slice3-container-postgres`, branched from **origin/main = 2f4efb0**
(verified by SHA; local main in the primary checkout is stale at f4c2fb4).
Spec: `docs/superpowers/specs/2026-09-17-gcp-deployment-design.md` §6, §8 slice 3.
Baseline measured here before any task: **3992 passed, 1 skipped** (96s) with
`FENCEAI_TEST_POSTGRES=postgresql://postgres:test@localhost:5432/postgres`.

**This directory is gitignored.** Slice 2's ledger lived in a gitignored worktree
and was one `git worktree remove` from being lost. Copy this one to
`docs/superpowers/ledgers/2026-09-23-container-and-local-postgres/` at the finish.

**Port discipline (user's standing constraint):** exactly ONE agent at a time may
run `tests/tools`, `tools/ui_smoke.py`, the compose stack, or the full suite.
Others get targeted suites (`tests/deploy`, `tests/api`, `tests/identity`,
`tests/web` bind nothing).

## Pre-flight scan

Cross-task rows (every pair sharing a file or an interface):

| Tasks | Produces → consumes | Finding |
|---|---|---|
| 1 → 3 | `tests/deploy/test_dockerfile.py` with `ROOT`, `_logical_lines()` → T3 appends `COMPOSE` and three tests using `ROOT` | Clean — T3's appended code only uses names T1 defines. |
| 1 → 2 | `Dockerfile` → `docker build` in `test_container.py` | Clean. |
| 1 → 3 | `Dockerfile` → compose `build: .` | Clean. |
| 1 → 6 | `Dockerfile` → CI `container` job | Clean. |
| 2 → 6 | gate variable `FENCEAI_TEST_DOCKER` → `env:` in the CI job | Clean — name identical in both. |
| 2 → 4 | T2's `test_it_boots_under_iap_on_a_database_with_no_dev_seed` runs BEFORE `dev_seed_lockout` exists | Clean by construction — it uses `FENCEAI_DB=/tmp/fresh.db`, so it passes before and after T4. T2 Step 3 already says so. |
| 4 → 7 | `dev_seed_lockout` → T7 Step 5 demonstrates the refusal on the compose database | Clean, and 4 precedes 7. |
| 4 → (existing) | `DEMO_ACCOUNTS` moves `api/app.py` → `identity/dev.py` | Clean — verified the only consumer is `_seed_demo_accounts` in the same file; `tests/api/test_user_admin_routes.py:33` mentions it in a comment only. |
| 5 → 7 | `target_base_url()` / `FENCEAI_SMOKE_BASE_URL` → T7 Step 3 | Clean — variable name identical. |
| 3 → 6 | `compose.yaml` → CI runs `pytest tests/deploy`, which reads it | Clean — the file is in the checkout. |
| 8 → all | docs only | Clean — no code interface. |

Self-consistency rows (each task's own text against itself):

| Task | Finding |
|---|---|
| 1 | **DEFECT — F1.** `_sync_lines()` filters on `"uv sync" in ln`, but the Dockerfile the same task specifies has **two comments** containing the words `uv sync` ("Both extras again — `uv sync` without them would…", and "`uv sync` installs the project as an editable pointer"). Those comment lines carry no `--extra`, so `test_every_sync_installs_both_extras` and `test_every_sync_is_frozen` FAIL on the very Dockerfile they are meant to pass. |
| 2 | **DEFECT — F2 (minor).** `image()` does not depend on `_require_docker`, so the build is ordered only by pytest's autouse-first rule rather than by a declared edge. |
| 3 | **DEFECT — F3.** `test_the_app_service_sets_no_dev_user` asserts `"FENCEAI_DEV_USER" not in compose.yaml`, but the `compose.yaml` the same task specifies contains that string **in a comment** explaining its own absence. The test fails against its own fixture. |
| 4 | **DEFECT — F4.** Step 10 predicts "4008 passed, 1 skipped". Arithmetic: 3992 + 6 (T1) + 3 (T3) + 10 (T4) = **4011**, and T2's four container tests SKIP without the gate, so the skip count is **5**, not 1. The plan's own self-review total (4015) is right about passes and wrong about skips for the same reason. |
| 5 | Clean — but flagged in the plan itself as the soft spot: `main()` is edited by description, mitigated by Step 7 (646/646 with the seam unused). |
| 6 | **DEFECT — F5.** Step 3 says "Expected: 10 passed" and then computes 13 in the same sentence. |
| 7 | Clean — runs and records only. |
| 8 | Clean — docs only. |

### Rulings

`Ruling: _sync_lines() filters on lines starting with RUN, not on any line containing "uv sync" (F1) — the property under test is what the image EXECUTES, and a comment is not an instruction; the alternative (stripping the explanatory comments from the Dockerfile) would delete the reasoning that makes the two-sync structure legible — costs nothing if wrong, since a RUN-prefixed filter still sees every real sync line.`

`Ruling: image() takes _require_docker as a parameter (F2) — an explicit edge instead of relying on pytest's autouse ordering, because a future reorder would otherwise attempt a multi-minute docker build before the gate is consulted — if wrong, it is one unused parameter.`

`Ruling: test_the_app_service_sets_no_dev_user asserts no line matches ^\s*FENCEAI_DEV_USER\s*: rather than absence of the substring (F3) — the real property is that the variable is not SET as a compose environment key, and the comment saying why it is absent is worth more than the simpler assertion; keeping the substring test would force deleting that comment, which is the thing most likely to be re-added by mistake later — if wrong, a future contributor could set the variable on a continuation line, which the compose schema does not allow.`

`Ruling: expected counts corrected to 4011 passed / 5 skipped at Task 4 Step 10, and 4015 passed / 5 skipped at the end (F4, F5); Task 6 Step 3 expects 13 in tests/deploy — and every task's instruction is to RECORD what pytest actually said and correct the plan in place when it disagrees, because a predicted count is a prediction and slice 2 lost three counts to exactly this — if wrong, the real number is in the task report either way.`

All five findings are rulings made against the plan's text before execution; each
is carried into the dispatch of the task it affects.

## Progress
Task 1: dispatched (sonnet), BASE=5c39018 — Dockerfile, .dockerignore, tests/deploy/test_dockerfile.py; carries rulings F1 (RUN-prefixed _sync_lines) and F4 (record real counts).
Task 1: implementer DONE (commit b7c8a7c) — 6/6 tests/deploy, full suite 3998 passed / 1 skipped in 88s, matching prediction exactly. Docker build clean first attempt; no README needed, as predicted. Implementer independently confirmed ruling F1 was necessary.
Task 1: Ruling: commit trailers name the model that actually wrote the commit (Claude Sonnet 5 for a sonnet implementer) rather than this session's Opus 5 — accurate attribution beats uniform attribution, and a fix round spent on a trailer is a fix round not spent on code — if wrong, the branch's trailers are inconsistent and nothing else.
Task 1: task reviewer dispatched (sonnet) on 5c39018..b7c8a7c.
Task 1: review clean — spec OK, quality Approved, no Critical/Important. Reviewer rebuilt the image itself and verified /app path identity across stages, the editable-install resolution, and NotoSansHebrew-Regular.ttf present at /app/src/fenceai/web/static/fonts with correct ownership.
Task 1: WARN resolved by me — running the image with no FENCEAI_DB crashes (sqlite3.OperationalError: unable to open database file), because /app is root-owned before USER fenceai and only .venv and src are chowned.
Task 1: Ruling: the crash STANDS and /app must NOT be made writable — a writable /app would let the SQLite fallback succeed into an ephemeral container layer, trading a loud boot failure for silent data loss of a salesperson's fence on the next revision. Spec 6 says a real deployment always sets FENCEAI_DB to a postgres:// URL. What is genuinely below this codebase's standard is the MESSAGE (compare provider.py, which names both admissible values), but making the app refuse an unset FENCEAI_DB would break every bare-uvicorn dev run and the whole suite, which both rely on the fenceai.db default — so: document it in the runbook's troubleshooting (Task 8) and require FENCEAI_DB in every docker run (Task 2). If wrong, an operator's first container run costs them one confusing traceback and a runbook lookup.
Task 1: complete (commits 5c39018..b7c8a7c, review clean)
Task 2: dispatched (sonnet), BASE=b7c8a7c — tests/deploy/test_container.py; carries ruling F2 (explicit image(_require_docker) edge) and Task 1's /app ruling (FENCEAI_DB mandatory in every docker run, do not chown /app).
Task 2: implementer DONE (commit c96c90e) — ungated 4 skipped; gated 4 passed in 3.45s (layer-cache hit, image ID 2b55a5d9e943 matching Task 1's build); asymmetric gate proved FAILING not skipping. docker ps -a clean before and after.
Task 2: task reviewer dispatched (sonnet) on b7c8a7c..c96c90e.
Task 2: review clean — spec OK, quality Approved, zero findings. Reviewer verified against SOURCE not docstrings: CMD's $PORT/0.0.0.0 discrimination, iap.py's eager 'import jwt' making the iap boot test a real extras proof, EXEMPT_PATHS covering /api/health, _wait_healthy's deadline and dead-container handling, and fixture teardown on mid-test failure. tests/deploy = 6 passed + 4 skipped ungated.
Task 2: complete (commits b7c8a7c..c96c90e, review clean)
Task 3: dispatched (sonnet), BASE=c96c90e — compose.yaml + 3 appended tests; carries ruling F3 (FENCEAI_DEV_USER asserted as 'not a compose env KEY' via regex, keeping the explanatory comment) and Task 1's /app ruling (compose sets FENCEAI_DB to the Postgres URL; do not chown /app).
Task 3: implementer DONE (commit a6dc98b) — tests/deploy 9 passed + 4 skipped, exactly as predicted. Stack up and healthy: app on 8080, db unpublished, 14 store tables, three seeded demo accounts present, no SQLite fallback file in the app container. Left running for Task 7.
Task 3: PLAN DEFECT found by the implementer — Step 6's illustrative query 'select id, email, capacity from users' is invalid: the store's tables are (id, email, doc) with doc a JSON text blob, per spec 10 ('every doc column is JSON text today and stays JSON text'). Implementer used doc::jsonb->>'capacity' and got the same evidence. No files affected; the plan's prose was wrong, not the code.
Task 3: Ruling: the same defect is latent in Task 7 Step 4 — I verified the live schema and there is NO 'runs' table; it is 'generation_runs' (projects is correct). Task 7's queries become 'select count(*) from projects' and 'select count(*) from generation_runs', carried in its dispatch. Caught by checking the running stack rather than by waiting for Task 7 to fail — if wrong, Task 7's implementer reads the error and fixes the query itself, costing one round trip.
Task 3: review — spec OK, quality Approved, but TWO Important findings, both inherited verbatim from my brief (implementer transcribed faithfully; these are plan defects, not implementation defects): (a) test_the_database_publishes_no_host_port checks only the literals '5432:5432'/'5433:5432', so long-form ports: or '15432:5432' would pass while still publishing the db; (b) test_the_app_waits_for_a_healthy_database checks 'condition: service_healthy' and 'pg_isready' as independent substrings, so a file with a sleep ALSO present, or the condition on the wrong service, would pass.
Task 3: Ruling: both findings are real and both get fixed now rather than parked — the tests exist precisely to catch a future edit, and a test weaker than its name is the failure mode this repo has already paid for twice this slice (F1, F3). This is the THIRD instance of one root cause: a substring assertion that the file's own explanatory comments can satisfy. Fix shape: a _service_block(name) helper that strips comments and blank lines and returns one service's lines, then assert the db block has no ports:/published: key at all (the stated property, not two collision examples) and that the app block waits on health, the db defines the healthcheck, and NO non-comment line in either service mentions sleep. Cost if wrong: one more round on a test file, and today's compose.yaml is already correct either way.
Task 3: fix round 1/5 dispatched — resumed original implementer with corrected assertions (_service_block helper stripping comments; db has no ports:/published: at all; app waits on health AND db defines it AND no non-comment sleep). Required to PROVE discrimination by making compose.yaml red three ways and reverting each.
Task 3: fix round 1/5 done (commit 0e4fb3c) — all three discrimination proofs went RED and reverted clean: 15432:5432 caught, 'sleep 10' on the app command caught, removed condition caught. compose.yaml byte-identical to a6dc98b afterwards. tests/deploy still 9 passed + 4 skipped.
Task 3: re-review — both findings ADDRESSED, no new breakage. Re-reviewer traced _service_block by hand (indent-0 resets prevent the top-level volumes: key leaking into the app block; nested db: at indent 6 is not mistaken for a service) and confirmed both blocks non-empty, ruling out the vacuous-[] failure mode. It reproduced discrimination itself with ports: ['15432:5432'] and reverted clean. compose.yaml byte-identical to a6dc98b.
Task 3: minor (deferred): the sleepers check is a bare case-insensitive 'sleep' substring over non-comment lines, so a legitimate value containing 'sleep' (e.g. an env var SLEEP_MODE) would trip it. Over-broad, not under-broad — biases to false failure, never false pass — so it cannot hide a defect. For the final review to triage.
Task 3: complete (commits c96c90e..0e4fb3c, review clean after 1 fix round)
Task 4: dispatched (sonnet), BASE=0e4fb3c — identity/dev.py gains DEMO_ACCOUNTS + dev_seed_lockout, lifespan refuses before writing anything, two new test files. Carries ruling F4 (real prediction is 4011 passed / 5 skipped, not the brief's 4008/1) and the four verified facts (single DEMO_ACCOUNTS consumer; no test enters TestClient twice; tests/api/conftest.py fresh DB per test is why under_iap survives; ids are hex so u_admin is unreachable by the real grant path).
Task 4: STALL — the implementer backgrounded the Step 8 pytest and stopped, waiting for a notification that never arrives. This is the documented slice-2 failure mode (seven subagents did it). Chased: verified on disk that all four files were present and NOTHING was committed, confirmed the pytest process was genuinely alive (not hung), and resumed the agent with an instruction to run foreground-only from Step 8. No work lost.
Task 4: implementer DONE_WITH_CONCERNS (commit 05ebcd3) — full suite 4011 passed / 5 skipped, exactly the F4-corrected prediction (the brief's 4008/1 was wrong, as ruled).
Task 4: CORRECTION to my own earlier read of the stall — I recorded the background pytest as 'genuinely alive, not hung'. It WAS hung, and the hang was a real defect in the code my plan specified: raising RuntimeError before lifespan's yield skips state.store.close(), so the open Postgres transaction from the list_users() read deadlocked the test's own DROP SCHEMA teardown forever. The stall and the bug were the same event; foregrounding the run is what surfaced it. Implementer added state.store.close() before the raise (single-test run went from hanging indefinitely to 1 passed in 0.39s).
Task 4: DEVIATION to review carefully — state.store.close() before the raise is beyond the brief's literal snippet. It is almost certainly right (in production the process dies anyway; in tests the held transaction blocks teardown), but the reviewer must check it cannot mask the lockout message if close() itself raises.
Task 4: review — spec OK, quality CHANGES NEEDED. Important: the unguarded state.store.close() can swallow the refusal. Reviewer proved it empirically (MASK surfaced 'closing a closed connection', lockout sentence absent, __context__ None) and found the house precedent in store/dialect.py:166-170 ('never let a failed rollback hide the real failure').
Task 4: Ruling: promote two Minors into the open fix round rather than deferring them, because both are in the SAME function, both are one-line message changes, and the operator sentence IS this function's deliverable: (a) a row caught by the id half at a company address produces a message claiming 'nobody can authenticate as an example.com address' while naming an address that is not one; (b) POST /api/users lets an admin create admin@example.com in a real iap deployment and there is no DELETE route, so the single remedy offered ('point FENCEAI_DB at a fresh database') would discard the company's data. Cost if wrong: a slightly longer refusal message.
Task 4: Ruling: KEEP both halves of the fingerprint rather than dropping the email half to remove the false positive. The id half alone catches every dev seed today, so the email half's only reachable false-positive is an admin deliberately creating an example.com row — but the belt still earns its place against a hand-restored or partially-migrated database, and the fix is to make the message name the offending rows and BOTH remedies, not to weaken the check. If wrong, an admin who creates admin@example.com on purpose meets a boot refusal that now tells them exactly which rows to remove.
Task 4: minor (deferred): build_provider() at app.py:117 raises after Store() has opened, leaking the same idle transaction; pre-existing, harmless in production (the process exits), and a try/except around lifespan's whole pre-yield body changes error semantics for every boot failure — too broad for this slice. For the final review to triage.
Task 4: minor (deferred): after the refusal, module-level state.store is closed while state.interpreter/proposer/critic/agent still belong to the previous boot. Safe today only because every API test re-enters TestClient. For the final review to triage.
Task 4: fix round 1/5 dispatched — guard the close (dialect.py precedent), rewrite the refusal sentence to name rows (id + email) and both remedies, annotate users as Sequence[User], refresh the dev.py module docstring, add two tests pinning the MESSAGE, and reconcile the plan doc's Step 7 snippet with the committed code. Required to reproduce the reviewer's mask experiment and show the guard holding.
Task 4: fix round 1/5 done (commit 7ecbf1e) — 4013 passed / 5 skipped. Mask experiment reproduced with close() forced to raise: the full lockout sentence still surfaces, naming all three rows as 'u_admin (admin@example.com)' etc. and both remedies. The guard holds.
Task 4: re-review — all four findings ADDRESSED, no new breakage. Re-reviewer independently reproduced the mask (monkeypatched Store.close to raise, dev-booted to seed, iap-booted): lockout RuntimeError surfaced with the full sentence and __context__ None. Confirmed TYPE_CHECKING import creates no cycle and no runtime import; plan doc's Step 7 snippet now matches committed code, nothing else in the plan altered. tests/identity/test_dev_seed_lockout.py 8 passed; tests/api/test_dev_seed_boot.py + tests/identity 72 passed.
Task 4: minor (deferred, FLAGGED FOR THE FINAL REVIEW TO TAKE UP): the close guard has no committed regression test — it was proven twice by throwaway scripts that were then deleted. A refactor removing the try/except would leave the suite green. This repository has already paid for exactly this failure: slice 2's app.js on('locale-changed', render) line had no test, so deleting it left the suite green with the refusal sentence frozen. Deferring rather than opening a new round, because the final review's single fix wave is the designed home for it; if it is not taken up there, it should be.
Task 4: complete (commits 0e4fb3c..7ecbf1e, review clean after 1 fix round)
Task 5: dispatched (sonnet), BASE=7ecbf1e — FENCEAI_SMOKE_BASE_URL seam in tools/ui_smoke.py. Told to read lines 4810-4940 in full before editing and preserve every comment verbatim (the plan's own declared soft spot: main() is edited by description, not by a rewritten listing). Foreground-only instruction included this time. Asked to run the seam against the live container as an early sanity check rather than leaving it all to Task 7.
Task 5: implementer DONE (commit 05c32ee) — MILESTONE. Smoke unattached 646/646 (seam invisible when unused). Smoke ATTACHED to the container on Postgres 646/646, with nothing listening on 8791 and no throwaway DB created, stack still healthy afterwards. Slice 3's core claim is proven ahead of Task 7's formal checkpoint.
Task 5: review — spec OK, quality Approved, three Minors. Teardown traced on every exit path and BOTH abort directions tested live (attached with nothing listening -> FATAL exit 2; unattached with a listener on 8080 -> FATAL exit 2); os.killpg(None.pid) proved unreachable. Comment freeze verified MECHANICALLY by diffing the multiset of comment lines: 0 lost, 0 paraphrased, 10 added. Five URL sites all converted; the sixth localhost:{PORT} is the own-port guard and correctly left literal. tests/tools + smoke-cases-registered 98 passed; architecture + deploy 25 passed / 4 skipped.
Task 5: ACTED — Minor 3 was operational and I fixed it in the controller session because it is worktree housekeeping, not code: the two smoke runs left 34 TRACKED tools/smoke-out/*.png dirty, and because the ATTACHED run was last they depicted the container's dev-seeded Postgres app. That directory is tracked and has historically been committed alongside feature commits (d20c5d1, 80df7b6), so a later slice commit could silently adopt container-sourced reference screenshots. Ran git checkout -- tools/smoke-out; tree now clean. The commit itself was always clean (exactly two intended files).
Task 5: WARN resolved by me — the reviewer flagged that the attached suite is no longer hermetic and that the deep-link check at ui_smoke.py:9110 navigates to a SEEDED id (#evidence=sref_...0001), which an IAP-mode container would not carry after 05ebcd3. It does not bite: compose runs FENCEAI_IDENTITY=dev, so the fixture is seeded, and the only IAP container in Task 7 is Step 5's refusal demonstration, which is EXPECTED not to boot and has no smoke suite pointed at it. Accumulated state across repeated attached runs is handled by Task 7 Step 1's 'docker compose down -v', which is exactly why that step exists. The 5s attached precondition is safe because Task 7 uses 'up -d --wait'.
Task 5: Ruling: promote Minors 1 and 2 into a short fix round rather than deferring — both are one-line changes in a file whose implementer is still live, and Minor 1 is genuine discoverability (the frozen comments rightly cannot be edited, so the module docstring is the only place left to say the seam exists; without it the next person re-invents it or hand-drives Chrome). Minor 2 is message-only and fails safe, but printing the raw env value makes a mistyped export self-explanatory for the price of one line. Cost if wrong: one extra round on a tool file, no behaviour change.
Task 5: fix round 1/5 done (commit 082fc0d) — both FATALs now print the raw exported value AND the resolved target: "FENCEAI_SMOKE_BASE_URL='/' resolved to http://localhost:8791, but nothing answered /api/health there". 98 passed. Browser suite deliberately not re-run (neither change touches an exercised path). Tree clean, no smoke-out PNGs dirtied.
Task 5: re-review — both findings ADDRESSED, no breakage. Re-reviewer reproduced the FATAL itself (raw value and resolved target both shown, exit 2, no listener left on 8791 or 9333) and confirmed BOTH frozen comment blocks untouched. 98 passed, tree clean.
Task 5: complete (commits 7ecbf1e..082fc0d, review clean after 1 fix round)
Task 6: dispatched (haiku — the brief carries complete YAML, so this is transcription plus one gated test run), BASE=082fc0d. Carries ruling F5 (13 passed in tests/deploy with the gate set, not the brief's self-contradicting '10').
Task 6: implementer DONE (commit 2a38ae3) — tests/deploy with the gate set = 13 passed (6 Dockerfile + 3 compose + 4 container), exactly the F5-corrected number.
Task 6: review clean — spec OK, quality Approved, zero findings. Reviewer parsed the workflow with PyYAML (jobs.keys() == ['pytest','container'], container is a flat sibling not nested under pytest.steps), confirmed bare uv sync genuinely collects all 13 (the only psycopg/jwt import runs INSIDE the image via subprocess, not on the runner), confirmed the pytest job is a pure append with its two --extra flags untouched, and confirmed _require_docker calls pytest.fail not skip. 13 passed gated, 9 passed + 4 skipped ungated.
Task 6: complete (commits 082fc0d..2a38ae3, review clean)
Task 7: dispatched (sonnet), BASE=2a38ae3 — the checkpoint. Carries three plan corrections: the generation_runs/projects SQL (no 'runs' table exists), -T on every docker compose exec (a TTY can hang a non-interactive session), and a mandatory 'git checkout -- tools/smoke-out' after the attached run so container-sourced reference screenshots are never committed. Told that down -v is load-bearing because two smoke suites have already accumulated state in that database.
Task 7: STALL #2 — the implementer backgrounded the smoke run and parked, DESPITE an explicit foreground-only instruction in its dispatch. This confirms the session note exactly: telling them to use the foreground does not prevent it; you have to check the worktree and chase. Verified myself that the run was genuinely progressing (Chrome up on 9333, screenshots being rewritten, stack rebuilt 10 min prior and healthy), so I chased WITHOUT restarting it — told the agent to poll its own BashOutput rather than await a notification, and to run everything after it in the foreground. No work lost either time.
Task 7: CHECKPOINT ACHIEVED. 646/646 browser checks against the container on Postgres. projects=24, generation_runs=20 (I re-verified both myself, plus stack health, rather than taking the report's word). Refusal demonstrated on the very database the trap applies to: exit 3, naming all three seeded rows and both remedies, with the long-running app service undisturbed and still healthy afterwards. Offline 3609 passed / 413 skipped, 0 failed. Dual-run 4017 passed / 5 skipped, 0 failed.
Task 7: count discrepancy RESOLVED (the implementer flagged it rather than silently reconciling, which is the correct discipline). 4017 is right and my '4013' expectation was stale — it omitted Task 5's four seam tests. Full accounting: 3992 baseline + 6 (T1) + 3 (T3) + 10 (T4) + 2 (T4 fix round's message tests) + 4 (T5 seam tests) = 4017 passed; 1 + 4 docker-gated (T2) = 5 skipped; total collected 4022, which reconciles exactly with the offline run's 3609 + 413 = 4022. Verified the two component counts directly (4 and 8).
Task 7: complete (no commit — this task produces evidence, not code). plan/current-status.md left MODIFIED and uncommitted by the implementer; Task 8 owns that file and will complete and commit it.
Task 8: implementer DONE (commits cb86871 + f873a9d) — architecture + locale 67 passed; contract hashes OK (contract.md and AMENDING.md, checked from inside docs/integration-contract/, which is the only way that check is meaningful).
Task 8: Ruling: MY dispatch was at fault, not the implementer — I wrote 'do not dispatch subagents' while its own brief Step 8 requires running architecture-critic and test-reviewer before the slice is declared done (CLAUDE.md mandates it). It resolved the conflict the way that served the project, and architecture-critic found a real wording defect it then fixed in f873a9d. In SDD that step is the controller's to run; the instruction should have said so. No rework.
Task 8: Ruling: architecture-critic's IntentConfirm.confirmed_by observation is REAL and I verified it myself (app.py:1032-1035 client-supplied default 'user' -> app.py:1043 -> intents.py:73 author=confirmed_by -> the decision graph). It is not a thirteenth finding; it is a THIRD SITE of finding 6, alongside Selection.author and Override.author. Recording it completes finding 6 rather than widening scope decided with the user — leaving it out would make the next slice fix two sites of three. The implementer was right to escalate rather than add it unilaterally. Disposition unchanged: travels with finding 1. Provenance to be stated honestly (found by reading, in slice 3, unlike the rest of the document).
Task 8: fix round 1/5 done (commit 588f4a0) — finding 6 now names all three sites with line references and honest provenance for the third. 67 passed, tree clean.
Task 8: Ruling: Task 8's dedicated task review is FOLDED INTO the final whole-branch review rather than dispatched separately — it is docs-only, and architecture-critic plus test-reviewer already ran over it (finding and fixing a real wording defect in f873a9d), so a third seat on the same prose would duplicate rather than add. If wrong, the final review covers the same files anyway. Deviation from per-task review discipline, recorded deliberately.
Task 8: complete (commits 2a38ae3..588f4a0, one fix round)
ALL EIGHT TASKS COMPLETE. Branch: 13 commits, 34 files, +7421/-44 vs origin/main 2f4efb0. Dispatching final whole-branch review (opus).
FINAL REVIEW: ready to merge. Reviewer reproduced the lockout end-to-end in the container itself, verified image contents (both extras, no .env, /app holds only .venv and src, uid 10001), the contract hashes from inside the directory, and confirmed NO remaining fixture-shaped test (both _sync_lines and _service_block strip the comments that would otherwise satisfy them). No Critical.
FINAL: Ruling: I1 gets FIXED NOW rather than inherited by slice 4. The finding is that POST /api/users accepts admin@example.com while no route can delete it — and my Task 4 ruling considered that case but framed it as 'a boot refusal an operator meets while deploying'. That framing was WRONG for the target platform: on Cloud Run lifespan runs on every new instance, so the row leaves the live instance healthy and makes every SUBSEQUENT instance fail to start — degrading on the next scale-out with no operator action and no warning, curable only by manual SQL against Cloud SQL. The guard exists at boot but not at the write that can trigger it. Fix is ~3 lines in the file that already has the answer. Cost if wrong: one more 409 on a path nobody should be using.
FINAL: Ruling: M7 (base image on floating python:3.12-slim while uv is digest-pinned) is DEFERRED, not fixed. The file's own reproducibility argument does apply, but digest-pinning a base image without a renovation process substitutes a staleness problem for a drift one, and both stages resolve to a single digest per build today. Recording the reasoning so the next person does not re-litigate it blind. If wrong, a base-image change lands unannounced between builds.
FINAL: fix wave done (commit c1a753a) — all nine items closed. Full suite 4022 passed / 5 skipped, +5 accounted for exactly (two new tests x2 backends under tests/api's dsn parametrization = +4, plus one Dockerfile test = +1). Image rebuilt with --no-dev: 'import pytest' now ModuleNotFoundError, websocket-client/pluggy/iniconfig/pygments absent, psycopg 3.3.5 and jwt 2.14.0 still import.
FINAL: I inspected the tests/web/test_locale_bundles.py change myself before dispatching the re-review, because editing an ENFORCEMENT test to accommodate a new code is how enforcement gets quietly weakened. It is the correct direction: the only change ADDS 'reserved_address' to REFUSAL_CODES with a comment, so both bundles are now REQUIRED to carry it. Enforcement strengthened, not relaxed.
FINAL: one scoped re-review of the fix wave dispatched (opus) on 588f4a0..c1a753a — the last gate. Told to check hardest that finding 1's 409 fires BEFORE save_user, uses the SAME predicate as the boot check so the two cannot disagree, and cannot refuse an ordinary company address.
FINAL: re-review — all nine ADDRESSED, ready to merge. Findings 1 and 2 MUTATION-PROVEN (deleting the guard reddens the 409 test on both backends plus the bidirectional locale scan; replacing the try/except reddens only test_the_refusal_survives_a_failing_close, confirming it is the sole cover). Guard verified to use the SAME function as the boot check (cannot drift), to fire before save_user, and to be unable to refuse a company address (hex-minted ids can never spell u_admin). Coverage checked at every other save_user site. he.json entry is genuine Hebrew (30/38 chars in the Hebrew block). Enforcement test: 5 insertions, 0 deletions, nothing relaxed. Contract OK. 4022/5 reproduced independently; +5 confirmed structurally via --collect-only, not just arithmetic.
FINAL: Ruling: one SMALL documentation-truth commit rather than merging the residue, and it is deliberately NOT a second fix wave of review findings. Three of the residual items are FALSE STATEMENTS, two of them introduced by the fix wave itself: (a) the httpx claim in spec 6's amendment, the Dockerfile comment and a test docstring — httpx is a transitive dependency of anthropic and is STILL in the image, so a reader infers --no-dev removed it when it did not, and the spec is the durable copy; (b) the plan doc at :1428 still says findings 2/4/5 all live in the IAP adapter, which is the origin of the very error we corrected elsewhere and will be re-read by whoever picks up slice 4; (c) the runbook's citation of ui_smoke.py:8929 moved to :8944 because of this wave's own edit. Plus plan/current-status.md has no entry for the wave at all, and CLAUDE.md requires that file to be current. Slice 2 shipped with three claims that had gone false and it cost a review cycle to find them; docs/ is where truth lives in this repo. Cost if wrong: one more small commit touching no code.
FINAL: deferred (may ship): test_every_sync_skips_dev_dependencies lacks the non-emptiness guard its sibling has (vacuous only if the Dockerfile stops using uv sync, which the extras test already catches); compose.yaml's 'written THREE times' is literally inexact but enumerates the right three; the reviews table files finding 5 under a row headed 'Slice 4 (IAP adapter / ADR-0013)', correct via the ADR half; the new guard is a no-op under FENCEAI_IDENTITY=dev by construction, which is the residual edge dev_seed_lockout exists to catch; no route deletes a user, so a row from a pre-fix deployment still needs manual SQL.
