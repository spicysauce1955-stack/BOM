# SDD ledger — plan: docs/superpowers/plans/2026-09-17-identity-is-googles.md

Spec: docs/superpowers/specs/2026-09-17-identity-is-googles-design.md (read, reachable)
Worktree: .claude/worktrees/identity-impl, branch feat/identity-is-googles, base e44377b

## Pre-flight scan

Pair rows — every pair of tasks sharing a file or an interface:

| Tasks | Produces → consumes | Finding |
|---|---|---|
| 1 → 2 | `Principal(email, subject)` | agree |
| 1 → 3 | `build_provider`, `Principal`, `DEV_COOKIE` | agree |
| 1 → 4, 6 | `DEV_COOKIE` in tests | agree |
| 2 → 3 | `bind(user, principal) -> ok/bound/mismatch` | agree |
| 2 → 9 | `User.subject` read by `peopleRows` as `bound` | agree |
| 2 ↔ 5 | both modify `identity/model.py` | sequential, no overlap in lines (2 adds `subject`, 5 removes the credential) |
| 3 → 4 | `POST /api/dev/identity` registered module-level under `dev` | agree |
| 3 → 6 | `require_admin`, `_public` | **CONFLICT — see R1** |
| 3 → 7 | `_actor(request)` with no fallback | agree |
| 3 → 8 | `GET /api/session` shape `{status,email,user,view,may_choose_view}` | agree |
| 3 → 10 | `EXEMPT_PATHS` = 3 paths | agree (corrected pre-merge) |
| 3 ↔ 5 | both modify `api/app.py` identity block | 3 rewrites routes, 5 removes the seed's password — disjoint |
| 5 ↔ 10 | `sessions` out of `_SCHEMA` vs the doc's table count | agree; 10 Step 3 updates the count |
| 8 ↔ 9 | both modify `index.html`, `app.js`, both bundles | sequential; 9 appends a tab, 8 replaces the form |
| 8 → 9 | `state.authStatus` / `state.authEmail` | agree |

Self-consistency rows — does each task's own text agree with itself:

| Task | Finding |
|---|---|
| 1 | agrees. `iap_identity_from_env()` imports no jwt, so `build_provider` under `iap` works without the extra — the test that asserts it is consistent |
| 2 | agrees |
| 3 | **three defects — R1, R2, R3, R4** |
| 4 | agrees |
| 5 | agrees |
| 6 | agrees |
| 7 | agrees |
| 8 | agrees. `DELETE /api/dev/identity` is added here and is the same exempt path as the POST |
| 9 | agrees (rendering half deliberately described, not specified — stated in the plan) |
| 10 | agrees |
| 11 | agrees |

## Rulings

Ruling: Task 3's `_public` KEEPS `exclude={"password_hash"}` until Task 5 deletes the field — the plan's Task 3 drops the exclusion while the field still exists. Why: between Task 3 and Task 5 every `/api/users` and `/api/session` response would serialize the scrypt hash. What it costs if wrong: nothing; Task 5 removes the exclusion when the field goes, and the docstring is rewritten there instead.

Ruling: Task 3's gate raises 403 unconditionally after `resolve` returns non-ok — the plan's `403 if user is not None or status == "no_capacity" else 401` can never reach its 401. Why: `no_identity` is already raised above when the provider returns None; every `resolve` refusal is an authenticated caller. What it costs if wrong: nothing, the branch is dead either way.

Ruling: Task 3 drops `test_a_subject_binds_on_first_arrival_and_is_written` — the plan itself marks it as deletable and its assertion (`in ("", "sub-1")`) asserts nothing. It is replaced by a test that a dev principal never WRITES a subject, which is the property that matters here. Why: `DevIdentity` carries no subject by design, so there is no dev path that binds one; Task 2 covers the decision and Task 1 covers the provider. What it costs if wrong: end-to-end binding is unproven until slice 4 meets real IAP.

Ruling: Task 3 also removes the now-unused `from fenceai.identity import session as sessions` import from `app.py`. Why: Task 3 deletes both routes that used it, and leaving it would make Task 5's deletion of the module a surprise failure. What it costs if wrong: nothing.

## Progress

Task 1: implemented (commit 088d1c6, base e44377b) — port, DevIdentity, IapIdentity, build_provider, `iap` extra.
Task 1: implementer reported 8/8 test_ports, 9/9 test_iap with the extra and a clean skip without it; offline import verified in a pyjwt-less venv.
Task 1: deviation disclosed — `uv.lock` committed alongside `pyproject.toml` (regenerated by `uv sync`, not hand-edited). Sent to the reviewer to judge on its merits, not pre-approved.
Task 1: brief defect noted by implementer — Step 5's "partial pass" is a collection error, because `test_ports.py` imports `build_provider` at module scope. Cosmetic; self-resolves by Step 9. Deferred minor against the PLAN, not the code.
Task 1: review dispatched (opus — authentication code, silent failure mode).
Task 1: review — spec ✅; quality CHANGES NEEDED. Adversarial checks PASSED: aud enforced, iss enforced, alg pinned (HS256-confusion and alg:none both return None), kid not steerable, every failure path fail-closed, cache not poisonable. uv.lock deviation judged correct to keep (purely additive, zero version churn).
Task 1: 3 Important — (a) no `exp` required, so a signed token omitting it is accepted for ever; (b) a gstatic blip past the TTL returns None for every request although a valid cached key is held, and does not advance _fetched_at; (c) no test pins the algorithm, so widening `algorithms` breaks nothing.
Task 1: ⚠️ resolved by me — `_fetch_google_keys` is exercised by nothing, and production gets PEM STRINGS from gstatic while every test hands in key OBJECTS. Ruling: this is a real gap and enters the fix round as a test — the two paths differ in type, and "verified out of band by the reviewer" is not a regression net. What it costs if wrong: one extra test.
Task 1: minor (deferred): blanket `except Exception` in iap.py:85 logs nothing, so a total auth outage reads as "everyone is nobody" with nothing for an operator to read.
Task 1: minor (deferred): provider.py:26 and iap.py:44 validate the same missing-audience condition with two different messages; the second is unreachable via build_provider().
Task 1: minor (deferred): key cache has no lock (cold start = N concurrent fetches) and an unknown kid does not force a refresh, so a rotation costs up to 1h of refusals.
Ruling: `DELETE /api/dev/identity` moves from Task 8 to Task 4, which owns the dev route. Why: it is the only backend line in an otherwise frontend-only task, and leaving it there would make Task 8 collide with every other agent touching api/app.py — which is what forces Tasks 5/6/7 to stay serial. What it costs if wrong: Task 8's `signOut()` calls a route that 405s until Task 4 lands; no test in Task 8 exercises it.
Ruling: parallel implementers each get their OWN worktree and branch, merged into feat/identity-is-googles as they land. Why: the skill forbids parallel implementers because they collide; the collision is the shared git index and shared files, not the parallelism, so isolating the worktree removes the reason rather than ignoring it. What it costs if wrong: merge conflicts I resolve, rather than corrupted commits I would not notice.
Task 2: dispatched in worktree identity-t2, branch feat/identity-task2, base 088d1c6 (haiku — complete code in the brief, 2 small files).
Task 8: dispatched in worktree identity-t8, branch feat/identity-task8, base e44377b (sonnet — multi-file frontend). Runs BEFORE Task 3 because its tests are a pure node check and static markup checks; it depends on Task 3's response SHAPE, which the plan pins, not on the route existing.
Task 2: implemented (commit 96b71fb, base 088d1c6, worktree identity-t2). 5/5 task tests; identity+api 315 passed.
Task 2: review dispatched (sonnet — small pure-domain diff), asked to prove the tests fail against a broken bind().
Task 2: review — spec ✅, quality APPROVED, no findings. Reviewer broke bind() two ways locally and confirmed both tests fail correctly (not vacuous), then restored.
Task 2: ⚠️ resolved by me (1/2) — "does the gate only persist on 'bound'?" is Task 3's code; my plan's `resolve()` saves only on "bound". Carried as a pointer into Task 3's dispatch.
Task 2: ⚠️ resolved by me (2/2) — the reviewer saw 285 Postgres tests SKIP. Cause: each worktree has its own .venv and identity-t2 had no postgres extra, so `tests/identity tests/store` ran 118 passed / 61 skipped. After `uv sync --extra postgres --extra iap`: 186 passed, 1 skipped. Verified by hand that `subject` round-trips on BOTH backends and that a legacy row with no `subject` key loads as "" — so the plan's claim that no migration is needed is now measured, not asserted.
Ruling: every future dispatch must say to run `uv sync --extra postgres --extra iap` and export FENCEAI_TEST_POSTGRES in its own worktree. Why: a fresh worktree's venv silently lacks the extras, and the Postgres half skips without failing — the exact "green means half a suite" trap the global constraints name. What it costs if wrong: a task reports green over an unmeasured backend.
Task 2: complete (commits 088d1c6..96b71fb, review clean) — merge into feat/identity-is-googles deferred until Task 1's fix round stops committing to that branch.
Task 1: fix round 1/5 — 4 findings sent, implementer reports all 4 addressed (commits 088d1c6..bd2b289). Disclosed judgement call: added _KEY_RETRY_BACKOFF_SECONDS=60 beyond the literal finding; sent to the re-reviewer to rule on rather than pre-approved.
Task 1: scoped re-review dispatched (sonnet) over 088d1c6..bd2b289.
Task 2: MERGED into feat/identity-is-googles as a1ad3cf (--no-ff, disjoint files, clean).
Task 3: dispatched in worktree identity-t3, branch feat/identity-task3, base a1ad3cf (opus — the highest-risk task: 689 previously-anonymous call sites now need an identity). Carried all four pre-flight rulings, the Task 2 reviewer's "persist only on bound" pointer, the venv/Postgres ruling, and an instruction NOT to touch the locale bundles because Task 8 owns them right now.
Task 1: re-review of fix round 1 — findings 1, 2, 4 ADDRESSED and verified load-bearing (reviewer stripped the `require` option and the test failed correctly; the two fake-clock stale-key tests genuinely exercise the retry). Finding 3 **NOT ADDRESSED**: the reviewer widened `algorithms` to ["ES256","HS256","none"] and all three tests STILL PASSED. They pass on PyJWT's own key-type guards (TypeError on an EC object as HMAC secret; InvalidKeyError for alg:none with a non-None key), not on this file's pin. Exactly the "passes against a broken implementation" defect. No new breakage; 62/62 identity tests pass.
Ruling: `_KEY_RETRY_BACKOFF_SECONDS = 60` is KEPT. Why: finding 2's own text named the per-request 5s-timeout hammering as part of the failure, so throttling is in scope rather than creep; the re-reviewer traced the two constants and found the fail-closed cutoff fires at most ~60s late and never starves, with rotation picked up on the first successful retry. What it costs if wrong: an auth outage is detected up to a minute later than the nominal deadline.
Ruling: Task 1 fix round 2 fixes finding 3 BOTH ways rather than choosing — add a test that captures the `algorithms` kwarg actually passed to `jwt.decode` and asserts it is exactly ["ES256"] (a real net against a future widening), AND correct the two attack tests' prose so nobody reads them as proving a pin they do not prove. Why: PyJWT's guards are genuine defence and our pin is defence-in-depth; the defect is the FALSE CLAIM about which one is being tested, and softening the prose alone would leave the pin with no regression net at all. What it costs if wrong: one slightly indirect test asserting a call-site argument rather than an end-to-end refusal.
Task 1: minor (deferred): cold-start branch (iap.py:81-87) has no backoff — during an initial-boot key-endpoint outage every request triggers its own blocking fetch. Correctness unaffected (everyone is None either way); wasted latency only.
Task 1: fix round 2/5 (finding 3 only; commit 43315ab). Implementer reports it widened the pin, watched `test_the_algorithm_pin_is_exactly_es256` fail with `['ES256','HS256','none'] == ['ES256']`, restored, and confirmed a clean diff. 63 passed against Postgres.
Task 1: scoped re-review of round 2 dispatched (sonnet) over a1ad3cf..43315ab — NOT bd2b289..43315ab, which would have swept Task 2's already-reviewed merge into the range. Re-reviewer instructed to redo the widening experiment itself, because the last two claims of a regression net on this finding were unverified.
Task 1: re-review of round 2 — finding 3 ADDRESSED, verified empirically by the re-reviewer: widening the pin now fails `test_the_algorithm_pin_is_exactly_es256` and ONLY that test, which is the correct signature. Confirmed the spy intercepts the real production call site (iap.py's local `import jwt` resolves to the same module object the test patches), so it is not a decoy. Corrected prose judged honest and not overcorrected. No new breakage; diff touched only the test file.
Task 1: complete (commits e44377b..43315ab, review clean after 2 fix rounds, 4 findings + 1 of my rulings all addressed).
Task 8: implemented (commit ef45df2, base e44377b, worktree identity-t8), DONE_WITH_CONCERNS. 822 passed in tests/web.
Task 8: deviation 1 — kept `error.sign_in_failed` in both bundles, because api/app.py still emits that code and test_locale_bundles.py::test_backend_code_list_is_current would go red. Sent to the reviewer to confirm the reasoning rather than accepted.
Task 8: deviation 2 — the brief's two `data-auth="denied"` CSS rules DO NOT WORK. A pre-existing higher-specificity rule kept #no-access hidden; the implementer verified in headless Chrome and added a third rule plus minimal styling. This is a defect in MY plan: the CSS was written blind, exactly the failure mode "plans assert behaviour, not fixtures" warns about. Sent to the reviewer to judge whether the added rule is the right fix or a patch over a wrong one.
Ruling: the `error.sign_in_failed` key and its REFUSAL_CODES entry are cleaned up when Task 3 lands, not in Task 8. Why: Task 3 deletes `POST /api/session` and with it the `sign_in_failed` code, and test_backend_code_list_is_current will demand the removal at merge — so the coupling surfaces as a red test rather than as something to remember. What it costs if wrong: a stale locale key in both bundles, caught by that test.
Task 8: review dispatched (sonnet).
Checkpoint: full suite on merged Task 1 + Task 2 (43315ab) against Postgres — 3850 passed, 1 skipped (the CI-only guard). Baseline before this slice was 3821, so +29 tests and nothing broken.
Task 8: review — spec ❌, quality CHANGES NEEDED, 2 Important.
Task 8: finding "Step 3's DELETE /api/dev/identity was never added and not disclosed" is INVALID, and the fault is mine. I ruled that route out of Task 8 and into Task 4 in the dispatch, but the ruling lived only in the dispatch prompt — the reviewer reads the BRIEF, which still carries the original step. The implementer followed my instruction exactly. Ruling: not a defect; the spec ❌ is withdrawn on this point. What it costs if wrong: nothing — Task 4 owns the route.
Ruling: when I move work between tasks, the move gets appended to the affected task's BRIEF FILE, not just the dispatch prompt. Why: the reviewer's three inputs are brief + report + diff, so a ruling that never reaches the brief reads to the reviewer as an undisclosed omission — it cost a full review cycle here. What it costs if wrong: nothing; it is bookkeeping.
Task 8: finding "the CSS fix has no regression test" is REAL and enters the fix loop. The reviewer deleted the implementer's own added rule and `tests/web/test_login_screen.py` still passed 8/8 — the brief's two regex assertions only pin the two rules the brief gave, not the third one that actually makes the screen render. A future edit can blank the no-access screen for every denied user with tests green.
Task 8: reviewer independently recomputed the specificity claim — pre-existing (1,1,2) beats the brief's (1,1,1), the added rule matches (1,1,2) and is declared later, so it is the right fix. Also confirmed by breaking them: the `view: null` trap test fails correctly when broken; `error.sign_in_failed` genuinely had to stay; email reaches the DOM via textContent inside <bdi class="sku">; logical properties only; no module-boundary violation.
Task 8: minor (deferred): report's closing line says "Not yet committed" but the work is committed at ef45df2.
Task 8: fix round 1/5 (commit 6f8f1bc). Implementer pinned BOTH that the reveal rule exists and that it is declared after the competing rule, and verified by breaking it two ways — deleted the rule (1 failed), and moved it before the competitor (failed on the ordering assertion specifically). 823 passed.
Task 8: scoped re-review of the fix dispatched (sonnet), told to redo all three experiments itself and to judge the ordering assertion for brittleness — a test that fails on a reformat costs more than it saves.
Task 8: re-review — finding ADDRESSED, all three experiments independently reproduced (delete → 1 failed; move before the competitor → fails on the ordering assertion at `assert 389 > 451`; restore → clean, 823 passed). Brittleness judged ROBUST: `_css()` strips comments before matching and the regexes use `\s+`, so reformatting and added comments cannot move it; its only fragility is exact selector text, which is this file's existing convention, not something the fix introduced. No new breakage.
Task 8: complete (commits e44377b..6f8f1bc, review clean after 1 fix round). MERGED into feat/identity-is-googles.
Task 8: POST-MERGE FINDING, found by me, missed by the review. `tests/web/test_session_module.py` was NOT a new file — it existed (created in d24e50d, extended in 25a24d3) and Task 8 REPLACED it, deleting 9 tests and adding 4. `pickProject` and `lastProjectKey` still exist in session.js and now have ZERO tests anywhere in the repo. The loss was invisible because the file-level count went 822 -> 823: tests added elsewhere in the task masked tests deleted here.
Task 8: root cause is mine — the plan said "create `tests/web/test_session_module.py` (create it if absent...)" because I believed the file did not exist. It did. The brief therefore never told the implementer to preserve what was there, and the reviewer checked the diff against that brief.
Ruling: reopen Task 8 for a fix round rather than carrying this forward. Why: deleted tests for surviving functions are real coverage loss, and the two functions concerned guard a salesperson's ability to get back into a job she was halfway through — the exact bug 25a24d3 was written to fix. What it costs if wrong: one extra round on an already-merged task.
Ruling: a task that rewrites an existing test file must have its deleted test names checked against surviving exports. Adding this as a standing check before I mark any remaining task complete — `git show BASE:<file> | grep "^def test_"` against the new file. Why: a net-positive test count hides a net-negative one inside a single file. What it costs if wrong: a few seconds per task.
Task 8: fix round 2/5 (commit c0e5a90) — 5 tests restored, 828 passed (was 823). My own name-level check: 9 old, 9 new, 4 old names still absent. Three are behaviour-covered by the new tests; the fourth (`test_signed_out_is_todays_app_and_not_a_locked_door`) is the untested `sessionState(no_identity).selector === true` that the implementer flagged itself.
Task 8: scoped re-review of round 2 dispatched (sonnet), asked to break each restored subject to prove the tests are nets, and to RULE on whether the fourth missing test is a real gap or a dead assertion — its premise changed, since signed-out used to mean the full app with a selector and now means the login screen.
Task 8: re-review of round 2 — ADDRESSED. All five restored tests proven load-bearing by breaking each subject in turn (table of five breakages, each failing its own test). grep now returns 10 hits for pickProject/lastProjectKey, was 0. 828 passed.
Ruling (adopted from the re-reviewer, who traced it rather than asserting it): the fourth missing test is a DEAD ASSERTION, not a gap. `sessionState(no_identity).selector` is true, but the value is provably inert — it reaches only `data-selector`, whose single consumer is style.css:857 targeting #view-select, whose ancestor <header> is already display:none whenever data-auth != "in", and no_identity never yields "in". What it costs if wrong: an unobservable field stays untested.
Task 8: complete (commits e44377b..c0e5a90, review clean after 2 fix rounds). MERGED.
Task 3: review — spec ✅; quality CHANGES NEEDED. 1 Critical, 5 Important, 8 Minor. Gate confirmed closed for all 73 APIRoutes; the FENCEAI_DEV_USER trap is genuinely avoided (reviewer neutered the gate and exactly the four anonymous tests failed); the deactivated-code split is right, mutation-proved both directions; the locale-bundle edit was REQUIRED, not optional; all eleven deleted tests accounted for (9 dead subjects, 2 renamed/split, 1 low-severity lost coverage).
Task 3: **C1 — `_bootstrap` can never fire in any deployment.** `lifespan` calls `_seed_demo_accounts()`, which writes `u_admin/admin@example.com/admin` into any empty users table; `_bootstrap` refuses if any admin row exists anywhere. So FENCEAI_BOOTSTRAP_ADMIN is permanently disabled and a real company's first admin has NO way in. Proved by the reviewer both ways.
Task 3: C1's root cause is MY SPEC, not the implementation. §3 of the design rules "`_seed_demo_accounts` stays, passwordless" and §6 rules bootstrap requires "no admin row exists anywhere". Both were my decisions and they are mutually exclusive. The implementer built exactly what both said.
Ruling: the seed becomes DEV-ONLY — `_seed_demo_accounts()` runs only under FENCEAI_IDENTITY=dev. Why: it is better than all three fixes the reviewer offered. The demo rows exist so a laptop and the browser smoke have personas, and dev mode never needs bootstrap because you can become anyone; under `iap` there is then no seed at all, so bootstrap works AND the spec's wart of shipping three inert strangers to production disappears rather than being argued for. What it costs if wrong: a dev laptop cannot exercise the bootstrap path without clearing the seed, so the bootstrap tests must build their own app state — which they must do anyway.
Ruling: the design spec §3 and §6 must be amended to record this, since the contradiction is in the document and not only in the code. Assigned to the docs task. What it costs if wrong: the next reader re-derives the contradiction.
Task 3: I2 — /openapi.json, /docs, /docs/oauth2-redirect and /redoc answer ANONYMOUSLY. They are Starlette `Route`s, not `APIRoute`s, so app.router.dependencies never reaches them. `GET /openapi.json` → 200 with the full schema while `GET /api/projects` → 401. This is the slice's own stated failure shape, one layer out.
Ruling: docs/openapi/redoc are disabled unless FENCEAI_IDENTITY=dev. Why: it matches the dev/prod split the slice already has, and keeps the local convenience that makes them worth having. What it costs if wrong: a production operator cannot read the schema from the service; they read it from the repo.
Task 3: I3 — half the bind() persistence rule is unpinned: hoisting save_user out of the `if` leaves 344 tests green, and since save_user writes an audit row that regression would be a DB write and an audit row PER REQUEST, drowning the log.
Task 3: I4 — the /api/dev/identity exemption is unpinned; deleting it from EXEMPT_PATHS leaves 290 tests green, because the test that should exercise it falls back to the seeded admin.
Task 3: I5 — tools/persona_lab/stack.py:114 still signs in with the password form and NO TASK IN THE PLAN OWNS IT. Ruling: assigned to the task that owns tools/ui_smoke.py. What it costs if wrong: the persona lab is broken and nothing says so.
Task 3: minor (deferred): CLAUDE.md's documented run command now dies at boot with no FENCEAI_IDENTITY, and CLAUDE.md is owned by no task — route to the docs task.
Task 3: minor (deferred): an INACTIVE admin row also disables bootstrap, so a deployment whose only admin was deactivated is locked out with no cure. Fail-safe; wants a sentence.
Task 3: minor (deferred): _bootstrap has no uniqueness guard — two concurrent first arrivals create two admin rows for one email.
Task 3: minor (deferred): unused imports EXEMPT_PATHS/require_admin at app.py:89; misleading comment at test_command_route.py:51; EXEMPT_PATHS not yet pinned by the fitness test (assigned later in the plan — confirm it is not lost).
Task 3: fix round 1/5 (commit cfbb24e). All five fixed; 664 passed in tests/api+identity, 3810 passed full suite minus tests/tools. Implementer's mutation results: reverting the seed guard fails 4 (both backends) and confirms the founder gets 403 no_capacity; hoisting save_user fails the new "arriving again writes nothing" test while the OLD identity_bound test stayed green under the same mutation — which is precisely why the new one counts save_user audit rows; deleting the dev-route exemption fails the rewritten test, which now runs on a `nobody` fixture instead of falling back to the seeded admin.
Task 3: the I1 tests are driven through a stand-in provider with provider_id="iap", because the dev provider is the case that seeds and therefore disables bootstrap — the bootstrap path is untestable under dev by construction. Sent to the re-reviewer to judge whether the substitution is faithful or tests a fiction.
Task 3: scoped re-review of the fix dispatched (opus — security centre of the slice), told to reproduce all three mutations itself rather than take them from the report.
Open question carried: a deployment that forgets FENCEAI_BOOTSTRAP_ADMIN now boots fine and refuses everybody with no hint the variable exists. Implementer proposed a one-line boot notice and correctly did NOT add it unasked. Re-reviewer asked to recommend log-vs-refuse-to-boot; I will rule once it answers.

## Standing instruction from the user (2026-09-17)
PUSH OFTEN — amended by the user from "push when done". Push the integration branch after EVERY task merge, and back up any unmerged task branch that holds real work. First push done at b69a60f (Tasks 1, 2, 8). `feat/identity-is-googles` is local-only so far; every task branch (feat/identity-task2/3/8) is local too. Final sequence: merge the remaining tasks, full suite + scenarios + architecture + browser smoke, both project reviewers, then push the branch and open the PR. Do not finish without pushing.

## REGRESSION found by me — `tests/tools` is RED on this branch, and it is NOT environmental

Measured, not inferred. Pre-slice code (worktree identity-design, zero src/ or tools/ diff vs origin/main): **89 passed**. feat/identity-is-googles (Tasks 1, 2, 8; Task 3 NOT merged): **67 passed, 22 errors**, reproduced from a verified-clean port state after killing every leaked process.

Both the Task 3 implementer AND the Task 3 reviewer called these errors environmental. Both were wrong, and I believed them for two rounds. The symptom is "port 9080 already in use"; the cause is three steps upstream:

`tools/persona_lab/stack.py:114` drives `document.getElementById('sign-in-password').value` — Task 8 DELETED that input from index.html. `.value` on null throws, the sign-in never completes, `stack()` raises "could not sign in as admin@example.com", and the uvicorn it started is never stopped. The leaked port makes every later test in the file error. One dead selector, 22 errors, not one of them naming the real cause.

Ruling: the fix goes to the task that lands `POST /api/dev/identity`, whose scope is extended to include repairing `tools/persona_lab/stack.py`. Why: the new picker's `become()` posts to that route, so fixing it now would swap one broken sign-in for another — the persona lab cannot work until the dev route exists. That task owns the route and is its first consumer. What it costs if wrong: tests/tools stays red one task longer, recorded here rather than forgotten.

Ruling: `--ignore=tests/tools` is BANNED from every further verification run on this slice. Why: I adopted that flag from a subagent's report and it hid a real regression across two review cycles. It is how "green" stopped meaning green. What it costs if wrong: slower runs while the lab is red.

Minor (deferred): stack.py leaks its uvicorn when sign-in fails — the failure path never calls stop(). Pre-existing, but it is what turned one broken selector into 22 errors plus a manual cleanup.
Task 3: re-review of fix round 1 — ALL FIVE ADDRESSED plus the /api/users gap, every mutation reproduced independently. The re-reviewer also ran a mutation the implementer had not claimed (hoisting save_user above the mismatch early-return) and confirmed BOTH halves of the persistence rule are pinned. _app_module_under judged safe by probing, not reasoning. _NotDev judged faithful, not a fiction — it satisfies the port exactly and the tests do not claim to exercise IAP verification, which is Task 1's. No assertions weakened; two strengthened. No new breakage.
Task 3: complete (commits a1ad3cf..cfbb24e, review clean after 1 fix round). MERGED as 2ff3b82 and PUSHED.
Ruling: the missing-FENCEAI_BOOTSTRAP_ADMIN case gets a boot-time LOG LINE, not a refusal to boot, and it gets a test. Why (adopting the re-reviewer's argument): unset is the SAFE state and a legitimate one — every deployment past its first admin runs that way — so this is a diagnosability problem, not a security one. Refusing to boot would also make the message harder to find: under a supervisor it becomes a restart loop and the one useful sentence scrolls past in a crash log. What it costs if wrong: an operator who ignores boot output still sees no_capacity with no hint.
Ruling: add a one-line consistency WARNING in lifespan when import-time _DEV disagrees with the built provider's provider_id. Why: the dev decision is only half single-sourced — docs and the impersonation route read _DEV at import, the seed reads provider_id at lifespan, and the fail-open direction (import saw dev, lifespan built iap) serves /openapi.json under IAP. Production divergence is nil because the environment is fixed before import, so a warning is proportionate; a raise would turn the five under_iap tests red. What it costs if wrong: an embedder importing the app before its environment is complete gets a warning rather than a refusal.
Ruling: Task 4's scope is extended to three extra items, because it owns POST /api/dev/identity and these are all that route or its consumers — (a) the DELETE route moved out of Task 8, (b) repairing tools/persona_lab/stack.py, (c) the two rulings above. What it costs if wrong: Task 4 is larger than planned and gets one review covering four concerns instead of one.
Locale task: implemented (commit c4733ca, base 2ff3b82, worktree identity-loc). 828 passed. The five error.* entries ALREADY existed in both bundles (Task 8 added them); the real and only gap was the guard — api/auth.py was not in the scanned list, so REFUSAL_CODES carried a comment about the debt instead of the codes.
Locale task: guard blindness demonstrated BOTH ways as asked — with the fix, deleting a key turns test_bundle_key_parity and test_refusal_codes_have_locale_entries red; replaying the same deletion against the pre-fix test file stays green at 51 passed. That is the guard proven, not assumed.
Locale task: concern raised that is sharper than the fix — 3 of the 5 codes (no_capacity, account_deactivated, subject_mismatch) reach their raise through `_REFUSAL_CODE.get(status, status)`, a VARIABLE, so the scanner still cannot see them even with the file added; they were parked in the `unscannable` set. Verified myself: no_identity and capacity_insufficient are literals at auth.py:137/156/163 and ARE scannable; the other three are not.
Ruling: rename `_REFUSAL_CODE` to a `*_CODES`-shaped module-level name so the scanner's table regex `^[A-Z][A-Z0-9_]*CODES\b...` reads its values, then drop the three-code exemption. Why: the table regex exists precisely to close this "by SHAPE rather than by adding one more file to the list", per its own comment — so the codes become visible structurally instead of by hand-maintained exemption, and the NEXT code added to that dict is guarded automatically. What it costs if wrong: three codes stay guarded only by the hand-maintained REFUSAL_CODES list, which is the status quo.
Ruling: that rename is sequenced AFTER Task 4 merges, and goes back to the locale agent rather than into Task 4. Why: Task 4 is mid-flight carrying four scope extensions plus the persona-lab repair, and auth.py sits close enough to its E4 work that a concurrent edit risks a conflict for no gain. What it costs if wrong: one extra small round.
Locale task: review — spec ✅, quality APPROVED, no findings above Minor. Reviewer reproduced BOTH guard experiments itself and read the Hebrew line by line: all five judged authored rather than transliterated, with subject_mismatch specifically checked for conveying "registered to a different Google account" without the words "subject" or "claim".
Locale task: my rename ruling CONFIRMED, and sharpened — the reviewer copied auth.py, renamed the dict, and ran the test file's actual `table` regex against it, extracting exactly the three exempted codes. Two conditions, both met by a pure rename: the name must start with [A-Z] (so the LEADING UNDERSCORE must go, which my ruling had not noticed) and must literally end in CODES. `_REFUSAL_CODE` is referenced in exactly two places, both inside auth.py, so the rename is self-contained.
Locale task: complete (commit c4733ca, review clean, no fix rounds). MERGED as b9e5a55 and PUSHED.
Task 9: implemented (commit c267ba6, base b9e5a55, worktree identity-t9). 832 passed in tests/web (828 + 4 new). Ran ahead of POST/PATCH /api/users deliberately, as Task 8 did.
Task 9: it fixed a hand-maintained view.js<->CSS mirror guarded by tests/web/test_view_sync.py that only the FULL-suite run surfaced — a scoped run would have missed it. Sent to the reviewer to confirm the fix is correct and did not loosen the mirror test to make something pass.
Task 9: design call — capacity labels reuse the existing signin.capacity.* keys rather than opening a people.capacity.* family, so zero new locale keys and the bundles stay 1515/1515.
Task 9: review dispatched (sonnet) with esc() as the adversarial focus — this panel renders user-controlled names and addresses, and its visitor is the one person who can grant capacities, so an XSS here is an XSS in the privilege-granting screen. Reviewer told to render a `"><img src=x onerror=...>` payload through the real render function and inspect the HTML rather than accept the implementer's own audit.
Task 9: concern raised by the implementer — GET /api/users has no capacity check beyond "signed in", so any authenticated account can list every person. Carried to the task that adds POST/PATCH. NOTE: `_public` returns the whole model, which after the password deletion includes `subject` (Google's sub). The panel needs only WHETHER it is set. Worth ruling on in that task rather than here.
Task 9: the implementer again labelled the tests/tools errors "environment issues, unrelated". Half right — they are not its task's, but they are a real regression in this slice. Third agent to reach for that label; it is a strong attractor and I should keep naming it in dispatches.
Task 9: review — spec ✅, quality APPROVED with one Important. esc() audit done adversarially by the reviewer with a real `"><img src=x onerror=...>` payload in name, email AND id: every field escaped, no attribute breakout from data-user. No XSS. All four node tests confirmed real nets by mutation. The view_sync mirror fix confirmed correct, minimal, and NOT a loosened test. Module boundary clean. Bundles 1515/1515.
Task 9: Important — people.js:104-110 comments claim a re-render "after either outcome" resyncs the select, but apiSend THROWS on non-OK so render() never runs on a refusal. A refused capacity change leaves the rejected value on screen until an unrelated redraw. Reviewer proved it against a stubbed 403.
Ruling: make the claim TRUE rather than correcting the comment down. Why: the comment describes the behaviour this panel ought to have, and this is the one screen whose job is to state who may do what — documenting "the select is not resynced on refusal" would be writing the bug down instead of fixing it. What it costs if wrong: one extra catch block on a handler that already alerts.
Task 9: fix round 1 dispatched. Told explicitly NOT to refactor the .toggle handler or the add form — the reviewer checked both and neither has the defect, so this is one handler and not a pattern.
Task 9: fix round 1/5 (commit ac4046f) — try/finally so render() runs on both outcomes, plus a real node-harness regression test that counts GET /api/users calls after a stubbed 403.
Task 9: re-review — ADDRESSED. Reviewer wrote its OWN node script (not the repo's test), confirmed the select ends on the server's value; reverted people.js to pre-fix and watched the new test fail at `assert 0 == 1`; restored clean; 833 passed. Judged `finally` better than `catch` and spotted that render() is deliberately NOT awaited inside it, so render()'s own rejection cannot mask the original apiSend throw — the classic try/finally footgun, avoided. Confirmed getUsersCalls can only be incremented by the finally's render(). .toggle and the add form unmodified. No new breakage.
Task 9: complete (commits b9e5a55..ac4046f, review clean after 1 fix round). MERGED and PUSHED.
Task 4: STALLED TWICE on background waits (291k tokens, two notifications saying "waiting for background work"). I chased it once; it stalled again. Rather than wait a third time I inspected the worktree myself.
Task 4: its uncommitted work looks substantially COMPLETE — E1 DELETE /api/dev/identity at app.py:2299; E2 stack.py drives only #sign-in-email, no password field; E3 the empty-table + unset-FENCEAI_BOOTSTRAP_ADMIN notice at app.py:167; E4 the import-vs-startup consistency warning at app.py:133; plus 104 new lines in tests/api/test_gate.py and a new tests/api/test_dev_identity_route.py. It never committed and never wrote its report.
Ruling: verify the work myself, then get it committed and reviewed rather than restarting the task. Why: the implementation is present and on the right files; a fresh implementer would redo work that already exists and would not inherit the reasoning behind it. Restarting is the answer to wrong work, not to unreported work. What it costs if wrong: the review catches what the missing report would have told me, one step later.
Task 4: committed at last (6f5787a) after I chased it. tests/api 626 passed (was 614). tests/tools 87 passed, 2 failed — from 67 passed + 22 errors. The port-leak cascade is GONE.
Task 4: I measured tests/tools INDEPENDENTLY on my own run: 87 passed, 2 failed, same two tests (test_persona_driver.py::test_outline_exposes_the_plan_canvas_rectangle_for_aiming and ::test_icon_only_controls_hide_their_tooltip_until_hovered). Agent's number corroborated rather than taken.
Task 4: it found and fixed a SECOND real defect while verifying — persona_lab/seed.py's raw urllib calls were being 401'd by the new gate. And it caught a test-poisoning bug in its own first draft: a literal importlib.reload(fenceai.api.app) took tests/api from 0 to 98 failures; fixed by loading an out-of-place module copy.
Task 4: the 2 remaining failures are a DIFFERENT regression, diagnosed but not fixed, and I approved stopping. Its reading: queue.js auto-switches an admin to the Jobs-queue tab on sign-in, racing stack.py's setTab('canvas'). Open question I put to the reviewer: tests/tools passed at 89 BEFORE this slice with that same auto-switch already present, so WHAT CHANGED? Hypothesis offered — deleting scrypt verification made sign-in dramatically faster and shifted the race. Need to know if this is a timing accident or a real behavioural change.
Ruling: Task 5 starts now, branched from Task 4's commit rather than from the integration branch. Why: Tasks 5, 6 and 7 all rewrite app.py and must be serial, and waiting for Task 4's review to finish would idle the critical path; branching from 6f5787a means no conflict and a merge order of 4-then-5. What it costs if wrong: if Task 4's review forces changes in app.py near the seed, Task 5's merge needs a hand resolution.
Ruling: every remaining dispatch must say to run verification in the FOREGROUND. Why: Tasks 4 and 5 both stalled holding for a backgrounded pytest whose notification never reached them — in both cases the work was complete and uncommitted, therefore invisible to me and at risk. Two agents, same failure, so it is the pattern and not the agent. What it costs if wrong: a few minutes of blocking per task, against a stall costing tens of minutes plus a chase.
Task 5: stalled the same way. Inspected the worktree myself: deletion looks COMPLETE — no password_hash/set_password/verify_password/DEMO_PASSWORD anywhere in src/, identity/session.py deleted, tests/identity/test_session.py deleted, `CREATE TABLE IF NOT EXISTS sessions` gone from the schema. Uncommitted.
Task 5: it also modified src/fenceai/project/queue.py, which is NOT in its brief's file list. Asked for the reason rather than assuming — a deletion legitimately reaches its callers, but an unexplained edit outside the named files is what a reviewer flags.
Task 4: review — spec ✅ (steps 1-4 and E1-E4 each verified individually), quality APPROVED, zero Critical/Important. Every new guard mutation-tested by the reviewer; none survived vacuous. The absence-under-iap property proved by mutating `if _DEV:` to `if True:`. The independent-module-copy technique confirmed not to leak: tests/api stays 626 whether the file runs alone or in the suite.
Task 4: minor (deferred) — stack.py leans on FENCEAI_DEV_USER as a server-side fallback to authenticate seed.py's cookie-less urllib calls. The reviewer judged this a DESIGNED use of the fallback (identity/dev.py's own docstring says the env var is what a bare uvicorn opens as) rather than a hack, and the cleaner fix needs seed.py, outside E2's authorized file. Acceptable as shipped; fragile only if the fallback's semantics change.
Task 4: complete (commits 2ff3b82..6f5787a, review clean, no fix rounds). MERGED as f175d54 and PUSHED.
Task 4: the race diagnosis is CONFIRMED structurally — setupHeader() registers queue.js's on("signed-in") -> synchronous setTab("queue") BEFORE wireIdentity()'s handler, and state.js's emit dispatches synchronously, so the ordering is real. OFFICE_ROAD never maps a step to the queue panel, and no other listener calls setTab("queue").
Task 4: the timing hypothesis is SUPPORTED BY ARCHAEOLOGY, not just plausible — queue.js's auto-switch landed in f24bf0d (2026-09-15), which PREDATES the password-based stack.py login (25a24d3, 2026-09-16). So the identical race structure coexisted with a green suite for about a day, under a deliberately-slow CPU-bound scrypt verification. Removing that verification is the only change to the sign-in path's timing profile. Conclusion: a previously-benign race started losing once sign-in got fast. Not new behaviour, newly lost timing.
Open: why stack.py's own LATER, explicit setTab('canvas') fails to stick is unresolved from source by two agents. My hypothesis for whoever takes it: stack.py polls `state.project` truthy, which may become true PARTWAY through openWorkspace(), so the stack's setTab runs before openWorkspace's remaining code (or a project-opened listener) switches tabs again. That needs live instrumentation to confirm, which I will authorize for that task.
Task 5: committed 4fe8346 after I chased it. Deletion complete and accounted for: queue.py edit was comment-only (a docstring referencing the deleted session.py's injectable-now pattern); every removed test name explained; grep audit clean; it also caught and fixed 04-backend.md's table count (15->14) as a consequence of dropping the sessions table, per CLAUDE.md's change-code-and-docs-together rule.
Task 5: its judgement on the open question — `subject` REMOVED from _public, replaced with `subject_bound: bool`. Reasoning accepted: GET /api/users reaches every signed-in capacity, nothing in web/static read the raw value, and handing a colleague's stable Google id to whoever asked is the same unforced surface-widening password_hash's exclusion existed to prevent.
Task 5: INTEGRATION BREAK caught by me, not by any test. people.js:33 reads `u.subject`, which _public no longer emits, so the panel would label every person "has not signed in yet" — including those who have. Task 9's node test cannot see it: its fixture supplies `subject` directly, so it passes whatever the API returns. The contract between _public and people.js is pinned NOWHERE.
Ruling: the break is mine — I dispatched Task 5 without telling it people.js consumed that field, and I dispatched Task 9 against a shape Task 5 was free to change. The plan's Interfaces blocks exist to prevent exactly this and I did not carry them between the two dispatches. What it costs: one fix round, and a contract test that should have existed from the start.
Ruling: Task 5 fixes its own consumer, and I lifted its web/static restriction to let it. Why: same principle as Task 4 owning the persona lab that called its route — the task that changes a shape owns what reads it. What it costs if wrong: a frontend edit from a backend-focused agent, which the review will catch.
Task 5: minor (deferred, NOT Task 5's): 04-backend.md says 73 routes and the app now serves 74 — drift from Task 4's dev-picker route. Assigned to the docs task.
Task 5: review — spec ✅, quality APPROVED, zero Critical/Important. Reviewer reproduced the grep audit exactly, accounted for all 9 removed test names (5+4, all genuinely dead subjects), and specifically VERIFIED the "covered elsewhere" claim for the deactivation property — confirmed live at auth.py:97 and asserted by two pre-existing tests it read rather than took on trust. Contract test verified both directions by the reviewer itself.
Task 5: the reviewer answered what the contract test MISSES, which is what I most wanted: bracket/computed access (u[key]), optional chaining, destructuring, spread, a renamed callback parameter, and any read outside peopleRows' own textual span. None apply today; they are latent brittleness in the extraction heuristic, not open holes. Worth knowing before anyone treats that test as complete.
Task 5: minor (deferred): the contract test's body extraction uses `src[start:].index("\n}")`, a textual heuristic that bounds peopleRows correctly only because its body has no column-0 closing brace before the real one. Wants a one-line comment stating the assumption.
Task 5: complete (commits f175d54..b576239, review clean after 1 fix round). MERGED as 29706b4 and PUSHED.
Task 5: reviewer closed its own "cannot verify" item afterwards — full suite from the identity-t5 worktree: 3918 passed, 1 skipped, 2 failed, 1 deselected. The 2 failures are exactly the two known tests/tools persona-driver tests; the deselected one is the route-count drift. Nothing else failed.
Task 6: dispatched in worktree identity-t6, branch feat/identity-task6, base 29706b4 (sonnet). Carried four things the brief cannot know: _public now sends subject_bound not subject; a contract test pins it; the consuming frontend is already merged and must not be edited; and every refusal code needs entries in both bundles now that the scanner reads auth.py.

## CORRECTION: the scrypt-timing explanation was WRONG

I recorded, and told the user, that the persona-lab race was "a previously-benign race that started losing once sign-in got fast", supported by archaeology showing queue.js's auto-switch predated the password login by a day. Two agents converged on that reading. It is wrong, and live instrumentation found the real cause.

`DevIdentity` is cookie-first, env-second. Task 4's own E2 fix put `FENCEAI_DEV_USER=admin@example.com` into the spawned server's environment (to stop seed.py's urllib calls being 401'd). That makes the PAGE already authenticated on its very first GET /api/session — dataset.auth goes pending -> in and never passes through `out`. So stack.py's unconditional form submission became a SECOND, redundant sign-in for the same account, racing the first.

Captured live via CDP rather than reasoned:
  t+3013ms  poll-saw-project-truthy   (satisfied by the AUTOMATIC sign-in)
  t+3014ms  tab-btn-active:canvas     (stack.py's setTab('canvas') took)
  t+3035ms  tab-btn-active:queue      (21ms later, the redundant sign-in's
                                        async completion fired queue.js)

So the cause was introduced by Task 4's E2 fix, not by deleting scrypt. My hypothesis (openWorkspace still having work left) was also refuted. The lesson: two agents plus me agreed on a plausible mechanism from source and archaeology, and all three were wrong — the instrumentation was worth authorising, and I should have authorised it one round earlier instead of telling the first two agents to stop chasing it.

Fix: skip the explicit form sign-in when the automatic identity already resolved to the target account; only drive the real form when it did not, preserving the "a broken login fails loudly" intent. No sleep, waits stay condition-based. tests/tools 87/2 -> **89 passed**, the pre-slice bar.

The prior candidate fix (click a queue row) was considered and rejected with a reason: a click between two sign-in cascades would still be overridden by the second cascade's completion. It would have made the number green without removing the race.
Race fix: review — spec ✅ (89 passed, independently reproduced in the foreground), quality CHANGES NEEDED, 1 Important + 1 Minor. Reviewer traced session.js apply() and app.js wireIdentity() and confirmed no window exists where dataset.auth === 'in' while state.me is stale, so the condition is sound and no case hangs; worst case is bounded and ends in the existing loud RuntimeError. The click-a-queue-row rejection holds up.
Race fix: Important — the fix BOUGHT GREEN BY NARROWING WHAT THE HARNESS DETECTS. The form submission was unconditional before; now it is skipped whenever the FENCEAI_DEV_USER fallback resolves to the target account, which — since stack.py sets that variable itself — is the common case. A break confined to the interactive picker path would sail past silently, while the comment still claims "a broken login fails here loudly". This is exactly the risk I named when dispatching it.
Ruling: fix it by REMOVING THE CAUSE, not by correcting the comment. Stop putting FENCEAI_DEV_USER in the spawned server's env and give seed.py its own dev cookie via POST /api/dev/identity. Why: that variable existed only to stop seed.py's raw urllib calls being 401'd, and Task 4's own reviewer flagged the workaround as fragile and named this cleaner fix, skipping it only because seed.py was outside that task's authorised files — it is inside this one's. One change does four things: the page starts genuinely signed out, so the form is exercised unconditionally again; the redundant second sign-in disappears at its ROOT rather than being detected and skipped; the loud-failure property returns in full; and a deferred fragility is closed instead of inherited. What it costs if wrong: seed.py grows a cookie jar.
Ruling: keep the `already` check even though it stops being the common path — it is correct defensive code for a stale cookie or a drifted variable, and it makes the harness honest about which path it took.
Race fix: told to PROVE the restored property by deliberately breaking the picker and confirming a loud failure, because an unverified claim that it is back would be the third unverified claim on this plan.
Task 6: review — spec ✅, quality APPROVED, ZERO findings. Reviewer reproduced every claim by running it, not reading it: mutated require_admin out of both routes (POST caught by 2 tests, PATCH caught ONLY by the test the implementer added beyond the brief); mutated _would_strand_the_admins; removed the fixture's deactivation and watched both last-admin tests fail against CORRECT code, proving the fixture change load-bearing. It also wrote 4 ad-hoc probe tests for walk-arounds (two admins demoted one at a time; editing an already-inactive row; deactivating the sole active admin while an inactive admin row exists) — guard held in all, scratch file deleted. 1490 passed.
Task 6: complete (commits 29706b4..12ea223, review clean, no fix rounds). MERGED as 9f5cbad and PUSHED.
Ruling: FIX the _bootstrap asymmetry — it counts any admin row, active or not, so a deployment whose only admin is deactivated is locked out permanently and the documented cure (FENCEAI_BOOTSTRAP_ADMIN + redeploy) silently does not work. The reviewer's downside analysis is what decided it: resolve() only calls _bootstrap for an email with NO existing row, so the change can never resurrect or re-promote the deactivated admin's OWN address — that address still hits its inactive row and gets account_deactivated exactly as today. The only behaviour change is for a different, row-less address matching the variable while zero admins are active, which is the stuck state the variable exists for. What it costs if wrong: leaving FENCEAI_BOOTSTRAP_ADMIN set after first deploy becomes load-bearing for both recovery paths, not just hygiene — and that must be said in the commit message.
auth fixes: review — spec ✅ both, quality APPROVED, zero findings. Reviewer ran the actual locale regex against the shipped auth.py (extracts exactly the three codes), reproduced both required mutations, and verified the crux by RUNNING it: reverting the .active filter fails the recovery test on both backends, while the deactivated admin's own address is untouched because resolve() only calls _bootstrap for an address with NO row and _bootstrap only ever creates. Deactivation still means something.
auth fixes: complete (commit 41c6de4, review clean, no fix rounds). MERGED and PUSHED.
DOCS DEBT (route to the docs task) — two spec files now contain FALSE claims as a direct result of this fix:
  - docs/superpowers/specs/2026-09-17-identity-is-googles-design.md:238-241 says bootstrap "self-disables the moment any admin exists". False: it self-disables only while an admin is ACTIVE.
  - docs/superpowers/specs/2026-09-17-gcp-deployment-design.md:280 carries the same stale phrasing, and its §6 frames removing the variable as best practice when it is now load-bearing for two recovery paths.
  A reader deciding "it self-disables anyway, safe to leave it set" would be wrong, and would leave a live re-entry path.
Race fix round 2 (commit 5d53595): FENCEAI_DEV_USER removed from the spawned server's env; seed.py now signs itself in via POST /api/dev/identity and carries the cookie on its own urllib calls. 89 passed.
Race fix round 2: it found a SECOND bug, and found it BECAUSE I demanded proof rather than a claim. Its first attempt to prove a broken picker fails loudly PASSED SILENTLY — because tests/conftest.py's autouse `_dev_identity` fixture monkeypatches FENCEAI_DEV_USER into os.environ for every test, so stack.py's `{**os.environ, ...}` silently re-inherited the variable under pytest even after it stopped setting it. The env dict now excludes that key explicitly. After the fix, the same deliberate break produced `RuntimeError: stack could not sign in as admin@example.com` both under pytest and standalone.
Note for the write-up: the claim "a broken login fails loudly" was TRUE standalone and FALSE under pytest. Either verification alone would have missed it. This is the strongest argument in the whole slice for requiring a demonstrated failure rather than an asserted property.
Race fix: re-review — ADDRESSED. Reviewer redid the break-it experiment itself (become() -> "refused" yields the loud RuntimeError at stack.py:197) and verified the env exclusion AT THE PROCESS LEVEL by inspecting /proc/<pid>/environ for both the uv wrapper and the child uvicorn — variable absent from both. seed.py's sign_in fails loudly on any non-2xx (HTTPError uncaught, nothing swallows it). test_persona_seed.py's edits adapt to the new mechanism with assertions unchanged, not loosened. No src/ change in the final commit. 89 passed.
Race fix: complete (commits 29706b4..5d53595, review clean after 1 fix round). MERGED and PUSHED. tests/tools is back to its pre-slice bar of 89.
Noted latent fragility (not blocking): FENCEAI_IDENTITY=dev reaches the spawned server only because tests/conftest.py sets it at import time and stack.py inherits it. Every caller of stack.start() today is under pytest. Running the lab outside pytest would hit the no-default refusal at boot — which is a loud failure, so acceptable.
Task 7: stalled on a backgrounded pytest — the THIRD agent to do so, despite the dispatch explicitly saying foreground. The instruction is not sufficient on its own; the pattern is strong. Inspected the worktree myself: all 11 `author: str` route params gone from app.py, `_actor` reduced to (request). Uncommitted. choices.js:259 still sends `author: "user"` — the second question, unanswered.
Note for the write-up: telling agents to verify in the foreground did not stop it happening. What actually works is what I have been doing — checking the worktree directly when a notification says "waiting", and chasing. Budget for that rather than expecting the instruction to hold.
Task 7: review — spec ✅, quality APPROVED, ZERO findings. Reviewer enumerated every BaseModel in the live app module for an `author` field, checked every _actor call site, all author usages across tests/tools/web, and confirmed no manual query_params or json() reads exist that could bypass DTO validation. Answer to "can a client still influence the audit actor anywhere": NO.
Task 7: the plan was WRONG about the shape and the implementer was right — 7 bare query params + 4 DTO fields, not 11 query params. Two of those DTO fields were live spoofing vectors on domain PROVENANCE beyond the audit fallback I described: Correction(**body.model_dump()) spread a client-supplied author into a stored field, and attributed_to=body.author did the same for KnowledgeVersion. My plan would have missed both.
Task 7: choices.js:259 investigated, not assumed — LIVE (feeds Selection.author, rendered "chosen by", never reaches the audit log). My assumption that it was dead weight was wrong; telling the agent to check rather than delete is what saved it.
Task 7: complete (commits 9f5cbad..2835a4d, review clean, no fix rounds). MERGED and PUSHED.
Task 10: review — spec ✅ on all steps and A1-A6, quality APPROVED with 1 Important. Reviewer re-ran everything and matched the claimed numbers exactly (3956/1 skipped, 310 scenarios, 89 tools, 15 architecture), verified both spec corrections land the required consequence sentence against the actual _bootstrap code, confirmed ADR-0013 follows ADR-0011/0012's shape and reads as a record not advocacy, and cross-checked ALL 13 ledger minors as accounted for with none silently dropped. The implementer also caught a self-contradiction in MY brief (it said EXEMPT_PATHS had four entries; it has three).
Task 10: Important — test_every_api_route_is_gated_by_the_app_itself is DECORATION for the exact escape class it exists to catch. It asserts app.router.dependencies is non-empty, a property of the app object that no bypassing route can falsify. The reviewer PROVED it by building both bypasses: a raw Starlette Route inserted into app.router.routes returned HTTP 200 with real data and no identity while the test passed; a mounted sub-app did the same AND is invisible to _route_paths() because getattr(r,"methods",None) is None for a Mount, so it evades the route-count and doc-table tests too.
Note: this is not a hypothetical class. This slice already shipped one instance — /openapi.json, /docs and /redoc answered anonymously for several commits for exactly this reason. A test whose docstring says it is "the only fix that cannot be forgotten" was a fix that could not fire.
Ruling: the test must assert a property of every ROUTE, not of the app — both halves: every APIRoute under /api carries the gate in its own dependency chain, AND nothing under /api is anything other than a gated APIRoute (no raw Route, no Mount shadowing an /api path), with the static mount at "/" named as the one explicit exception and why it is safe. Verified by the reviewer's own two experiments. What it costs if wrong: the slice ships a fitness test that reports green while the class of hole it names is open.
Ruling: fix the run command in docs/v1-runbook.md but not the file's unrelated staleness — it is this slice's own breakage, same category as CLAUDE.md's, and a runbook whose documented command dies at boot serves nobody. Leave docs/superpowers/plans/ alone: plans are point-in-time records here, and back-editing one blurs the only honest record of what was thought at the time.
Task 10: fix round 1 (d58de4f) replaced the decorative gate test with two per-route tests; re-review verified ALL THREE escapes caught (raw Route, Mount, and a dependency-stripped APIRoute — the third being one the first reviewer had not tried), each by the right test with a message naming the offending path. Static-mount exception judged named-and-reasoned rather than a blanket skip. v1-runbook narrowly scoped.
Task 10: fix round 2 (db5b1d1) — the re-review found breakage introduced BY the fix: ADR-0013 cited test_every_api_route_is_gated_by_the_app_itself by name, which that fix had deleted, and credited it with the exact property the review proved it never delivered. Corrected to record what was actually learned, including why it took two tests and the slice's own prior instance of the gap.
Ruling: I accepted fix round 2 on my OWN verification rather than dispatching a fourth review agent for 19 lines of ADR prose — I confirmed both named tests resolve to real functions, and read the paragraph against the code. Recorded rather than silently skipped, and the final whole-branch review covers the ADR as part of the branch. What it costs if wrong: a prose inaccuracy that the final review is positioned to catch.
Task 10: complete (commits 0789a6e..db5b1d1, review clean after 2 fix rounds). MERGED as b4779c2 and PUSHED.

## All eleven plan tasks plus three out-of-plan fixes are merged. Remaining: Task 11.

## SESSION END — 2026-09-22

State: branch `feat/identity-is-googles` at b4779c2, fully pushed, main unchanged since the branch point. 37 commits. All 11 plan tasks + 3 out-of-plan fixes merged, each reviewed, 14 fix rounds total across them.

Verified on the merged branch: full suite 3957 passed / 1 skipped / 0 failed (dual-run against Postgres) · scenarios 310 · architecture 16 · tests/tools 89 (pre-slice bar) · browser smoke 643/643 · clean-clone offline boot ok.

NOT DONE — Task 11, the whole of it:
  1. `architecture-critic` agent (CLAUDE.md requires it for slices touching domain abstractions).
  2. `test-reviewer` agent (same).
     BOTH MUTATE CODE. Re-run the full suite after each; green at 3957 beforehand says nothing about afterwards.
  3. The final whole-branch review (superpowers:requesting-code-review), on the most capable model, pointed at this ledger's `minor (deferred)` and `parked` lines so it can triage what must be fixed before merge.
  4. `plan/current-status.md` — not updated for this slice.
  5. THE CHECKPOINT: the product owner watching it run. Green tests are not the checkpoint.
  6. No PR opened. Deliberate — the reviewers mutate code, so the PR body should describe the finished slice.

Stale worktrees left in place, not cleaned, because I could not account for their contents and they are not mine to guess at:
  - `.claude/worktrees/store-dialect-shim` — modified gcp spec + an untracked identity spec draft, both differing from main AND from this branch. Slice 1 leftovers.
  - `/home/user/.superset/projects/BOM-login-first` — another session's worktree with modified smoke PNGs. Not mine; leave alone.
  - Task worktrees identity-t2..t10, identity-race, identity-auth, identity-loc: all merged, all clean, safe to `git worktree remove` once the PR lands.

## Task 11 — resumed 2026-09-22 (session 2)

Correction to the SESSION END block: the branch's base is `origin/main` = e44377b. LOCAL
`main` is stale at f4c2fb4 (slice 1 landed upstream as PR #10 and the local ref was never
moved), so `git diff main...HEAD` reports 146 files and sweeps in slice 1's store-dialect
work. The slice is `git diff origin/main...HEAD` = 110 files, +3974/-870. My first dispatch
of the architecture-critic carried the wrong base and was corrected mid-flight.

architecture-critic: verdict **SOUND-WITH-FIXES**, 1 Important + 5 Minor + 1 declared.
It proved rather than asserted: broke the gate two ways (a raw Starlette `Route` and a
directly-constructed `APIRoute`, both answering 200 anonymously) and watched the right
test fail each time; swept every parameterless route under both providers and found
exactly `EXEMPT_PATHS` answering; added a refusal code and watched the locale guard fail.
Clean verdicts recorded: the port is a real port (three importers outside `identity/`,
none in `strategy/`/`fulfillment/`/`decisions/`/`report/`), `generate_route` takes no
`Request` so ADR-0004 purity is untouched, the dev provider is an adapter not a second
path (iap = 75 routes, only non-APIRoute is the static Mount), `capacity` is the domain
vocabulary rather than an authz vocabulary bolted beside it.

**F1 (Important) — FIXED in 6f05e05.** `ReviewBody.reviewer` was client-supplied and
reached `KnowledgeVersion.attributed_to` AND the audit actor. Confirmed by me in the code
before acting. The critic missed one consequence and I found it: `js/tabs.js` hard-coded
`reviewer: "expert-admin"`, so no rule change in the shipped product has ever been signed
by the person who made it. Root cause is the guard test: it pinned four DTO names and the
field name "author", written from a grep of what was already fixed, so it could not catch
the name nobody thought of. Replaced with an enumeration of every BaseModel defined in
`api/app.py` against a set of actor-naming words, plus an end-to-end forgery test. Both
fail when the defect is reinstated, both backends, each on its own subject.

**F2, F3, F5, F6 — FIXED in 6f05e05**, each with a net proven by reverting the fix:
declared `authStatus`/`authCode`/`authEmail` in `state.js`; the no-access screen renders
which refusal this is (`GET /api/session` now carries `code`, so the browser keeps no copy
of the status→code mapping) and the refusal test is the complement of in/out rather than a
list of three; `stack.py` tells the spawned server `FENCEAI_IDENTITY=dev`; the dead
`error.sign_in_failed` key and the two `not_signed_in` client fallbacks are gone.

Ruling: F3's server half adds a `code` field to `GET /api/session` rather than mapping
`deactivated` → `account_deactivated` a second time in JavaScript. Why: that mapping's own
comment in `REFUSAL_STATUS_CODES` says the two registers differ deliberately, and a copy
in another language is a second place to forget. `tests/web/test_session_module.py`'s
key-set assertion caught the addition, which is the test working: updated to include
`code` and to assert it is null for a resolved caller, with the reason written down.
What it costs if wrong: one more field on a shape three test files assert against.

**F4 — DEFERRED to the final whole-branch review** (Minor, recommendation: fix).
`auth.resolve` logs `subject_mismatch` on every refused request, and the gate calls it on
every request: 5 arrivals measured = 5 audit rows, so an unauthorized caller grows a
domain artifact at request rate. Worse, the row's actor is `actor_ref(user)` — the log
says `user:u_dana` PERFORMED `subject_mismatch` when Dana did nothing and somebody else
arrived on her address. `identity/model.py` is explicit that an actor means who performed.
My recommendation is `actor="system"` with the offending subject in the ref, but it
changes audit semantics and this slice has one review left that can rule on it.

**F7 — NOT A DEFECT, but say it out loud.** Capacity is enforced on two routes. Measured
as Dana (`sales`): `GET /api/audit`, `POST /api/knowledge`, `POST /api/knowledge/.../retire`
and `GET /api/users` all answer 200; only `POST /api/users` is 403. ADR-0013's Consequences
section names this honestly, so it is declared, not hidden. Worth carrying into the next
slice's spec as its starting point: a salesperson can rewrite the rule base that generates
every other project's fence, which touches §15 "versioned knowledge" harder than the read
routes the gate now covers.

Full suite after the critic and the fixes: **3968 passed / 1 skipped** against Postgres
(was 3957/1). Committed 6f05e05 and PUSHED.

test-reviewer: verdict **GAPS — F1 blocks the PR**. 26 mutations applied, measured and
reverted; it ran the full suite, the scenarios and the browser smoke itself. 22 mutations
were CAUGHT and it named the catching test for each (gate refusals, `bind`, `_bootstrap`
both directions, `require_admin`, `_would_strand_the_admins`, `aud`/`iss`/algorithm pin/
grace window, the raw-Route and Mount escapes) — so the suite is load-bearing where it is,
including the two fitness tests and the algorithm-pin test this slice already repaired.

**F1 (HIGH) — FIXED.** `subject_mismatch` was never refused THROUGH A REQUEST anywhere.
Widening the gate to `if status not in ("ok", "subject_mismatch")` handed every route in
the app to a swapped Google account and left the whole suite green at 3968. The three
tests that looked like coverage all call `resolve()` or `bind()` as functions, with
`make_gate` nowhere in the path — and `tests/api/test_gate.py`'s own docstring says a
refusal nobody has seen fire is a refusal nobody has tested. Every OTHER refusal code does
have a request-level test; this was the one that did not. Three tests added on the
existing `under_iap` fixture; the reviewer's exact mutation now fails, and only it.

**F3 (MEDIUM) — FIXED, and it was MINE, introduced 6f05e05 an hour earlier.** I wrote
`REFUSAL_STATUS_CODES[status]` in the session route where the gate uses
`.get(status, status)`. A fourth `resolve` status would 500 the one route whose entire
purpose is to answer somebody the rest of the API refuses — while the browser test I added
in the same commit asserts that fourth status is handled. The suite was green on a
property the server would not survive. Note for the write-up: the fix and the hole arrived
in the same commit, written within minutes of each other, and neither review that preceded
it could have caught it because it did not exist yet. This is the argument for running the
reviewers AFTER the fixes, not only after the tasks.

**F2 (MEDIUM) — FIXED.** `test_an_unknown_key_id_is_nobody` was vacuous: its hand-built
token omitted `iat`, which is in the `require` list, so PyJWT refused it before the `kid`
lookup mattered. A `_key_for` falling back to "any key we hold" passed it — the reviewer
proved it resolved a Principal with a bogus kid. Rebuilt on the complete-claims helper
with a positive control. Third instance of a vacuity class this file had already fixed
twice for HS256 and `alg:none`; the author caught two and missed this one.

**F6 (LOW) — FIXED.** Only `exp` of the six required claims had a test, and `iat` is
guarded by nothing else, so its silent removal from the list was invisible. Parametrised
over all six; dropping `iat` now fails exactly `[iat]`.

**F5 (MEDIUM) — FIXED by removing the reason the tests were brittle.** The reviewer pushed
back on two tests I had added an hour before, and was right: one failed when the two `!==`
operands were swapped, the other when a duplicated expression was hoisted into a `const` —
both behaviour-preserving, and the second refactor is strictly BETTER because the reason
line defaulted a null code to `no_capacity` while the advice line compared the raw code, so
a code-less refusal read one sentence and hid the advice for another.
Ruling: move the decisions rather than loosen the regexes. `isRefused`, `refusalTextKey`
and `showsAskAnAdmin` are now pure exports of `session.js` beside `sessionState`, executed
in node over every status including one that does not exist yet; `app.js` only writes the
answers to the DOM; `test_login_screen.py` keeps only the markup facts a static check can
honestly hold. Why: the property was worth pinning and the regex was the wrong instrument —
this is the frontend half of the same lesson as the `app.router.dependencies` fitness test,
which asserted a property of the app object rather than of every route. Verified both ways:
reverting `isRefused` to a list of three fails, making the advice unconditional fails, and
swapping the `!==` operands — which broke the old test — now stays green.
Also fixed the pre-existing `html.index("</section>")` slice, which found the document's
FIRST closing tag rather than this section's.

**F4 (MEDIUM) — PARTLY FIXED.** The per-refusal sentence was rendered by nothing that
executes it: `grep -c "no-access-reason" tools/ui_smoke.py` was 0. Added a check to the
smoke case that already drives the denied screen. The branch that HIDES the generic advice
still cannot be driven from a browser (it needs a deactivated row or a swapped subject,
neither of which the picker can produce) — node covers those three, and that split is
written into both tests.

**F7 (LOW) — DEFERRED to the final whole-branch review, with a recommendation NOT to fix
it blind.** `tools/ui_smoke.py`'s `wait_for` lets a CDP evaluation error RAISE instead of
counting as "not ready yet", so a transient `document.documentElement` being null
mid-navigation aborted the reviewer's first of two runs (`643/643` on the second).
Pre-existing, untouched by this slice. It is one line, but the line converts a loud crash
into a poll timeout, and this harness is what every other claim on this branch is verified
with — a second opinion is worth more than the hour it saves.

Full suite after all of it: **3981 passed / 1 skipped** against Postgres (was 3968/1).

Final whole-branch review (opus, read-only + probes): **ready to merge WITH FIXES**, no
Critical. It reproduced every number in this ledger exactly (3981/1, 97.79s) and verified
rather than relayed — which is what it was asked to do, and it mattered: **four of the ten
deferred findings I handed it were already fixed or inaccurately described.**

Triage outcome:
- #1 refused-caller audit rows — FIX BEFORE MERGE. Both halves confirmed by probe. FIXED.
- #2 capacity on two routes — ACCEPT AND RECORD, with two corrections to MY description:
  capacity IS enforced in a third place (the command door, `commands.perform(capacity=...)`),
  and the §15 damage is bounded because knowledge versions are immutable, attributed, and
  retired rather than destroyed — loud and reversible, not silent. "Declared" is enough for
  a deployment with no users on it; it would not be for a live one.
- #3 smoke `wait_for` — NEXT SLICE, and the asymmetry runs the OTHER way from what I
  assumed: a blanket try/except turns a genuinely broken selector into a mute timeout, which
  is how a smoke suite starts lying. Narrow it and print the last error in the detail.
- #4 blanket `except` logs nothing — UPGRADED from Minor and FIXED. It made it concrete:
  a padded `FENCEAI_IAP_AUDIENCE` (a newline from a YAML block or a secret) boots clean,
  announces `identity provider: iap`, and refuses every valid assertion, silently.
- #5 key cache — SPLIT: unknown-`kid`-no-refresh is the largest availability risk in the
  adapter (a scheduled Google rotation = up to 1h of company-wide refusal) → next slice;
  lock/backoff accepted. It also found a FOURTH item I had not listed: the grace window is
  not enforced between retries, so keys past the declared correctness boundary verify for
  up to 60s at a time. Proved with a fake clock.
- #6 `_bootstrap` uniqueness — ACCEPT, and my stated consequence was FALSE: four racing
  threads produced one row and one `IntegrityError`, because `users.email` is NOT NULL
  UNIQUE and both racers bootstrap the same address by construction. The real cost is one
  unhandled 500 on a first arrival where a retry succeeds.
- #7 two messages, one condition — ACCEPT, don't unify; the real defect hiding beside it
  was the missing `.strip()`.
- #8 stack.py uvicorn leak — ALREADY FIXED in d20c5d1. I was carrying a stale entry.
- #9 forgotten `FENCEAI_BOOTSTRAP_ADMIN` — ALREADY FIXED (boot notice, Task 4 E3).
- #10 docs debt — ALREADY FIXED across all four surfaces. My caution ("the ledger says it
  was assigned, not that it was done") was right to state and wrong on the facts.

Also fixed before merge, beyond the ten: **CI never installed the `iap` extra**, so 22
tests — the whole production adapter suite — skipped behind `importorskip` in the pipeline
that gates merges, green. And a container built as gcp spec §6 prescribed (`uv sync
--frozen`, no extras) would have booted healthy and raised `ModuleNotFoundError` out of the
gate on every route, because `principal()` imported `jwt` lazily. Both closed.

Ruling: `Selection.author` / `Override.author` stay client-named for now, but the FALSE
DOCSTRING does not. My own guard test said the server fills them from `_actor`; it never
has — `put_choice` and `add_override` store the body verbatim, `js/choices.js` sends the
literal "user", and `strategy/generator.py` puts both into decision-graph nodes as `author`
and `chosen_by`, where the decision graph IS the explanation. Why fix only the docstring:
filling them from `_actor` changes a domain field's meaning ("who chose this") in the
generator and the rendered prose, which is a slice, not a patch at the end of another one.
What it costs if wrong: a caller can still name who chose, for one more slice, on a field
that was never an audit actor. A guard test that records a false fact as its reason for not
looking is how the next reviewer stops looking too — that half was not deferrable.

Remaining open, all recorded for the next slice's spec, in the order the reviewer put them:
per-capacity authorization · unknown-`kid` refresh · a `dev`-seeded database run later under
`iap` is permanently locked out (the `@example.com` admin row keeps `_bootstrap` disabled and
nobody can authenticate as it; recovery is database surgery) · the grace-window-vs-backoff
bug · the smoke harness's `wait_for` · `POST /api/knowledge` 500s on an unknown `type`.

Full suite: **3984 passed / 1 skipped**. Browser smoke: **644/644** (up from 643 — the new
check is the no-access screen's reason line). Committed 0c59e7a and PUSHED.

REMAINING: the checkpoint (the product owner watching it run), then the PR.

## PR #13 opened, then five concurrent reviews (2026-09-22)

PR: https://github.com/spicysauce1955-stack/BOM/pull/13, base `main` (= origin/main e44377b).
Five agents in parallel: the two unreviewed commits · frontend i18n/RTL/XSS · docs accuracy
and diff hygiene · adversarial security · a validation battery that OWNED every port-binding
run so the other four could not collide with it. That split is the thing to reuse: the
port-collision failure mode on this branch is well documented, and one owner plus four
read-mostly reviewers avoided it entirely.

**BLOCKER 1 — the last-admin guard lost to ordinary concurrency, unrecoverably.** The check
read `list_users()`, the write was a separate store call, so two concurrent PATCHes each saw
the OTHER admin as the one still standing and both committed. 41 of 60 unassisted trials.
`_bootstrap` only admits an address with NO row, so `FENCEAI_BOOTSTRAP_ADMIN` cannot rescue
either stranded admin: the cure is a new address and a redeploy, or database surgery.
Ruling: the check and the write become ONE store call (`Store.amend_user_guarded`), with the
policy as a pure function in `identity/model.py`. Why that shape: `apply_review_outcome` is
the same shape for the same reason, so it is the codebase's own precedent rather than a new
idea; and `@_serialized`'s docstring already names this TOCTOU class and defers it, so the
deferral had to be broken deliberately and narrowly rather than by widening the decorator.
What it costs if wrong: one store method that knows the word "admin". The test races two
demotions through a `threading.Barrier` and fails 3/3 runs against the old code.

**BLOCKER 2 — a first arrival silently reverted an admin edit it never saw.** `resolve` read
the row, `bind` mutated the copy, and the write sent that whole copy back — a snapshot taken
before the admin's PATCH. A deactivation during a first sign-in was undone, with the admin
told it worked. 15 of 60. `bind_subject` re-reads under the lock and writes one column. The
test stages the interleaving rather than racing it, so it fails every time.

**BLOCKER 3 — the operator log line I added 0c59e7a was an INJECTION SINK.** PyJWT
interpolates an unsupported `crit` header verbatim, and that header is read BEFORE the
signature check, so anyone reaching the service could write arbitrary multi-line text into
our WARNING log, formatted as our own logger at any level. The comment above the line already
said "exception name only, never the token"; the code logged `str(exc)`. Note for the
write-up: this is the SECOND defect introduced by a fix for a previous review's finding, on
the same branch, in the same file family. The lesson is not "be careful" — it is that a fix
round needs its own review seat, which is exactly what this round gave it.

**BLOCKER 4 — `GET /api/session` wrote an audit row per poll.** Exempt from the gate, so it
answers 200 and costs a refused caller nothing, and the no-access screen fetches it on every
load. 50 polls, 50 rows. `resolve(..., audit=False)` for the reporting caller.

Frontend (nothing blocking, two fixed): a locale flip drew the whole company roster into a
salesperson's DOM, undoing the laziness the module's own comment promises; and
`app.js`'s `on("locale-changed", render)` — the line that makes the refusal sentence follow
the toggle — had NO test, so deleting it left the suite green with the refusal frozen. Fixed
by giving the no-access screen its own language button (the header's is hidden there, so a
refused English speaker had no way to read it) and driving it from the smoke suite.
Verified in a browser by the reviewer, not read: HE/EN both correct on every element.

Docs: three claims had become FALSE — `04-backend.md` said the command door is "the first and
only gated route" in a table whose adjacent row says every route is gated; `05-frontend.md`
described `/api/me`, a reload on sign-out, and one gated route; `v1-runbook.md` said `dev`
opens as `admin@example.com`, which is a default in `.env.example` and nowhere in the code.
Contract freeze verified INTACT (hash checks, no diff, header still v1.3). No secrets.

**CI HAS NEVER PASSED, and it is not this branch.** All three runs on `main` fail at the same
step, four days before this branch existed. Cause found in the logs rather than guessed:
`stack.start()` slept a fixed 4s then made ONE unguarded request to Chrome's CDP port. Cold
runner, connection refused, the uvicorn leaks its port, every later test errors with "port
8940 already in use" — 22 errors reporting a collision, none naming the browser that had not
started. Condition-based wait now, plus `--disable-dev-shm-usage` and a per-run profile.
Ruling: fix it here rather than merging red and filing it. Why: it is four lines of the same
failure this slice has twice paid for (a fixed sleep instead of a condition), the logs point
straight at it, and a merge gate nobody can trust is the same defect as a green suite that
proves nothing. What it costs if wrong: CI stays red and the fix is still correct.

Validation battery at 48b1f17, all green: full suite 3990/1 Postgres · 3584 passed + 407
skipped offline (every skip Postgres-gated) · scenarios 310 · architecture 16 · tools 90 ·
browser smoke 646/646 · clean clone installs without either extra, boots on `dev`, and
refuses to boot with no `FENCEAI_IDENTITY` (exit 3, loud). After the CI fix: 3992/1, tools 90
in 28s (was 42 — the fixed sleeps were most of it).

Also corrected: three stale counts this branch had itself written (the suite numbers in
CLAUDE.md were the previous slice's; the `test_iap.py` count was bumped 22 → 25 by a commit
that then added two more). The count that cannot stay true is gone, and the suite numbers now
name the commit they were measured at — a number with no vintage goes stale silently, which
is how all three did.

## MERGED — 2026-09-22

PR #13 merged to main as 2f4efb0. `origin/main` now carries the identity port; `identity/session.py`
is gone from main, verified after the merge rather than assumed.

**CI went green at 088db4f — the first passing run this repository has ever had.** The Chrome
startup race was the whole of it: the two runs before the fix failed, the run with it passed
in 5m35s. Worth keeping as the lesson: CI had been red since the day it was added, nobody had
read its logs, and the cause was four lines of a fixed `time.sleep` that a cold runner lost.
A merge gate nobody trusts decays into a merge gate nobody reads.

Cleanup done: 13 merged task worktrees removed and their branches deleted (identity-t2..t10,
identity-race, identity-auth, identity-loc, identity-design). LEFT ALONE, deliberately, as the
session-start notes say: `.claude/worktrees/store-dialect-shim` (locked, slice-1 leftovers
differing from both main and this branch) and `/home/user/.superset/projects/BOM-login-first`
(another session's).

`identity-impl` is kept: this ledger lives in it and is gitignored, so removing the worktree
deletes the only record of the slice's rulings. It should be copied somewhere durable before
anyone removes it.

Local `main` in the primary checkout is still stale at f4c2fb4 — another session has it
checked out, so it is not mine to move. Anyone working there should `git fetch && git merge
--ff-only origin/main` before branching, or they will branch from four days ago, which is the
trap that made a 146-file diff look like this slice's.
